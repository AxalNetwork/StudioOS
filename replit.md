# Axal StudioOS
An API-first Venture Studio Operating System designed to manage the entire startup project lifecycle from intake to portfolio monitoring.

## Run & Operate
- **Run (dev)**: `npm run dev` (frontend) and `python backend/main.py` (dev backend).
- **Build**: `npm run build`
- **Deploy (production)**: `npm run deploy` (deploys Cloudflare Worker)
- **Typecheck**: `npm run typecheck`
- **Codegen**: _Populate as you build_
- **DB Push**: _Populate as you build_
- **Required Env Vars**: `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_CLAIM_EMAIL` (for web push notifications).

## Stack
- **Frontend**: React 19, Vite 6, Tailwind CSS 4
- **Production API**: Cloudflare Worker (Hono on Workers, TypeScript)
- **Dev Backend**: FastAPI (Python)
- **ORM**: _Populate as you build_
- **Validation**: _Populate as you build_
- **Build Tool**: Vite

## Where things live
- `frontend/`: React frontend.
- `cloudflare-worker/`: Production API source code.
  - `cloudflare-worker/src/index.ts`: Main Worker entry point.
  - `cloudflare-worker/src/routes/*.ts`: API routes.
- `backend/`: FastAPI development backend (never deployed).
  - `backend/app.db`: SQLite database for dev backend.
- `scripts/check-api-drift.mjs`: Script to check API ↔ Worker drift.
- **DB Schema**: `cloudflare-worker/src/schema.sql` (for D1) and `backend/app.db` (SQLite for dev).
- **API Contracts**: Defined implicitly by Cloudflare Worker routes and FastAPI routes.
- **Theme Files**: `frontend/tailwind.config.js`

## Architecture decisions
- Production API is a Cloudflare Worker for performance and scalability, while FastAPI provides a faster local development loop.
- Strict separation of production (Cloudflare D1) and development (SQLite) databases.
- Frontend logging and storage handled via `safeReadJSON`/`safeWriteJSON` and `reportError` for robust error handling and data integrity.
- Theme/density use a CSS-variable system in `frontend/src/index.css` (`--app-bg`, `--app-surface`, `--app-text`, `--app-input-*`) under `:root` and `[data-theme="dark"]/.dark`, plus Tailwind 4 `@custom-variant dark (&:where(.dark, .dark *))`. SettingsContext flips both `data-theme` and the `.dark` class on `<html>`. Body, inputs, autofill, scrollbars, and native `<select>` panels flip automatically; new pages should add `dark:` Tailwind variants on hardcoded `bg-white`/`text-gray-*` tokens. Density rules target `[data-card]` (Settings cards) and `[data-density-target]` (page wrappers).
- API ↔ Worker drift is prevented by a CI script, ensuring consistency between frontend and backend contracts.
- Rate limiting is implemented per bucket, and strict CSP headers are enforced for security.

## Product
- AI-driven startup scoring engine.
- Legal entity formation (Spin-out Wizard, Incorporation Flow, Co-Founder Agreement).
- Fundraising tools (Cap-Table Simulator, Fund Management, Reserve Allocation, Exit Waterfall Simulator).
- Portfolio monitoring (Portfolio Health Score, Predictive Failure, Watchlist + Decision Journal).
- Compliance management (Compliance Calendar, 83(b) Tracker).
- Founder support (Wellbeing check-ins, Mentor Matching, Office Hours, Co-founder Matching).
- Partner engagement (Partner Office Hours, Co-Marketing, Public Partner Directory).
- Financial modeling (Financial Model Builder).
- Mobile PWA with offline capabilities and push notifications.

## User preferences
The user prefers clear and concise communication. They value iterative development and expect the agent to ask for confirmation before implementing major architectural changes or significant code refactoring. The user also requests that the agent prioritizes security best practices and robust error handling in all implementations.

## Gotchas
- The FastAPI backend is *only* for local development and is never deployed. Do not attempt to deploy it to Cloudflare.
- API ↔ Worker drift is a common issue; always run `npm run test:drift` or ensure CI passes before merging.
- Admin role changes require direct SQL modifications, as there is no UI for this.
- KYC is optional until critical legal documents require it, but ensure the system handles its eventual enforcement.
- After a deploy, apply pending one-time migrations: `wrangler d1 execute studioos-db --file=cloudflare-worker/sql/<file>.sql --remote --env=""`. As of 2026-05-08 all known migrations through T3/T11/T12/T13-15 + perf_indexes + backfill_activity_user_ids + lp_investors_seal + `migrations/001_progress_tables.sql` + `migrations/002_user_settings.sql` + `migrations/003_admin_analytics.sql` are applied to remote D1. Pending: `migrations/004_subscription_plans.sql`, `migrations/005_plan_currency_and_fx.sql`, `migrations/012_analytics_snapshots.sql`. (Spin-Out Lab `spinout_lab.sql` was applied to remote D1 on 2026-05-08 — adds `spinout_lab_milestones` table + `users.spinout_lab_active|_week|_started_at` + `users.is_incorporated`. Re-runs will report "duplicate column name" on the 4 ALTERs, which is expected.) (Note: re-running `t13_t14_t15.sql` will error "duplicate column name: mentor_id" on the ALTER — D1 rolls the whole file back, but every CREATE in it is `IF NOT EXISTS` so the file is effectively idempotent except for that ALTER.)
- 2026-05-06 prod deploy: worker version `5848f64d-9082-4461-9436-2867fc8e8688` (T22.1 hashed-actor activity logs + T12 incorporation auto-seed live). Same day, the 7 patch/minor Dependabot PRs (#7,#8,#9,#10,#14,#19,#21) were rebased and squash-merged into main; main HEAD `f01c39b`, all CI green. Remaining open Dependabot PRs are all major-version bumps — see project tasks #3 (hono+postcss), #4 (protobuf 4→7), #5 (defer majors).
- 2026-05-06 Task #3: postcss bumped to ≥8.5.10 via npm `overrides` in `frontend/package.json` (resolved to 8.5.14). hono alert was already fixed by Task #2's worker group bump (4.12.18 ≥ patched 4.12.14).
- 2026-05-06 Task #4: `protobuf` bumped 4.25.9 → 7.34.1 in BOTH `requirements.txt` and `pyproject.toml`/`uv.lock` (uv would otherwise resolve to 6.x — pyproject.toml needed an explicit `protobuf>=7.34.1,<8` pin). Removed the `replit==4.1.2` package entirely from requirements.txt — it pinned `protobuf<5` and was never imported anywhere in the codebase. All current google libs (`google-api-core 2.30.3`, `proto-plus 1.27.2`, `googleapis-common-protos 1.74.0`) accept `protobuf<8.0.0,>=4.25.8` so no transitive bumps were needed. Backend boots clean, Dependabot alerts #6 + #12 will auto-close on next rescan.
- 2026-05-06 Task #5 (deferred majors): Between Task #4 wrapping and Task #5 starting, four major-bump PRs were already merged into main and verified clean: **react 19.0→19.2.6 + react-router 7.1→7.15 (#6)**, **recharts 2→3.8.1 (#16)**, **jose 5→6.2.3 (#11)**, **typescript 5→6.0.3 (#13)**. Worker `tsc --noEmit` and frontend `vite build` both pass; both audits report 0 vulnerabilities. Two majors were deferred; **PR #15 lucide-react 0→1 is now done (Task #6, 2026-05-07)** — bumped to ^1.14.0 in `frontend/package.json`. v1 dropped the brand icons `Linkedin`/`Twitter`/`Github`, which were inlined as local SVG components in `frontend/src/pages/PublicProfilePage.jsx` and (for `Linkedin` only) in `frontend/src/pages/ReferEarnPage.jsx` (which already had its own `LinkedinSvg`; `LinkedinIcon` now aliases that). All 141 other imported icon names were verified to still exist in v1 and `npm run build` is green. **PR #12 build group / vite 6→7 is now done (Task #7, 2026-05-07)** — bumped `vite` to ^7.0.0 (resolved 7.3.3) and `@vitejs/plugin-react` to ^5.0.0 in `frontend/package.json`. Node baseline is 20.20.0 (≥20 required by vite 7). `vite.config.js` needed no changes; `npm run build` and `npm run dev` both pass cleanly, frontend audit reports 0 vulnerabilities. Dependabot alerts #4 (high) + #5 (medium) will auto-close on next rescan. No remaining deferred upgrades.
- T12 cofounder NDA reuses the existing `documents` table (status enum: draft|generated|sent|signed). Worker schema lacks `signed_ip` — the connection row stores it instead (`nda_signed_ip_a/b`).
- T12 captable engine is a faithful TS port of `backend/app/services/captable.py`; CSV export served as `text/csv` with `Content-Disposition: attachment; filename="captable-<name>.csv"`.
- T12 incorporation auto-seed of compliance events is now wired into the worker. `cloudflare-worker/src/routes/compliance.ts` exports `STANDARD_EVENTS` + `seedStandardEventsForJurisdiction`; `legal.ts` `/incorporate/wizard` calls it after entity creation, wrapped in try/catch (calendar failures must never block incorporation), and returns the new rows in `seeded_compliance`. Idempotent via `INSERT OR IGNORE` against the existing `UNIQUE(project_id, event_type, due_date)` constraint — re-incorporating the same jurisdiction is a no-op.
- T11 wellbeing data uses AES-GCM (WebCrypto) keyed off `AXAL_ENCRYPTION_SECRET || JWT_SECRET` — ciphertext is NOT interchangeable with the FastAPI Fernet rows, by design (D1 and SQLite are separate stores). Helper: `cloudflare-worker/src/services/cryptoBox.ts`.
- T11 `/api/financials/:id/export.xlsx` returns `text/csv` (worker has no XLSX lib); the frontend download helper auto-renames via `Content-Disposition` filename, so no client change needed.
- `activity_logs.actor` now stores a 16-hex `email_hash` (SHA-256 truncated) for ALL writes: register / referral / verify / login + every admin write in `routes/admin.ts` + KYC events in `routes/kyc.ts` + commission/payout events in `routes/network.ts` + profile-capture/verify in `routes/profiling.ts` + contract resend/void in `routes/admin_contracts.ts` — never the plaintext email. Use `user_id` for joins instead. The shared helper lives at `cloudflare-worker/src/util/hashEmail.ts`. Activity reads in `routes/activity.ts` still LOWER(actor)=LOWER(email)-match for backward compat with legacy pre-Epic-11 rows. T22.1 also strips plaintext target emails out of admin KYC `details` columns (e.g. `kyc_document_access`, `profile_verified` now reference `user_id=N` instead of the target's email).
- T4 (Epic 11) ops items NOT in code, owner=user: (a) disable R2 public access + add 90-day lifecycle rule to Standard-IA, (b) verify search/backfill cron in prod, (c) apply any remaining pending D1 migrations via `wrangler d1 execute studioos-db --remote --file=…` (see Pending list above — `lp_investors_seal.sql` installs RAISE triggers so D1 itself rejects writes to the deprecated table).
- CI now runs `npm audit --omit=dev --audit-level=high` for both frontend and worker, plus `pip-audit` for the dev backend. High/critical CVEs in prod deps fail the build; dev-dep advisories don't.
- Paginated reads (`/api/activity`, `/api/admin/users`, `/api/marketplace/syndication`, `/api/dashboard?days=N`) are clamped via `cloudflare-worker/src/util/pagination.ts` (limit defaults vary; max 200 / 50 / 365 days).
- Frontend toasts must use `useToast` (auto-clears on unmount) — raw `setTimeout(setToast, …)` leaks state updates into unmounted components.
- Modals must call `useEscapeClose(onClose)` for keyboard dismissal; bind once near the top of the component.
- T9 — `SCORING_HMAC_SECRET` is hard-required in production (`ENVIRONMENT=production`); the worker refuses to boot without it (≥32 bytes). Dev/preview fall back to `JWT_SECRET` with a one-shot startup warning. Provision before next prod deploy: `openssl rand -hex 32 | npx wrangler secret put SCORING_HMAC_SECRET --env=production`.
- T10 — Unified calendar fully ported to the worker (`routes/calendar.ts` + `services/calendar.ts`, schema in `cloudflare-worker/sql/calendar.sql`). Both Google Calendar and Microsoft 365/Outlook push-sync ship as side-by-side providers (status / connect / callback / sync / disconnect). OAuth `state` is HMAC-signed off `JWT_SECRET` and persisted single-use in `oauth_state_tokens` (10-min TTL, opportunistic sweep). `calendar_sync_records` provides idempotency: re-syncs PATCH the existing remote event instead of duplicating. Refresh tokens are encrypted at rest via `cryptoBox` (AES-GCM keyed off `AXAL_ENCRYPTION_SECRET || JWT_SECRET`) before being written to `google_oauth_tokens.refresh_token` / `microsoft_oauth_tokens.refresh_token`; the read path in `services/calendar.ts:loadRefreshToken` decrypts on the way out and lazily re-encrypts any legacy plaintext row on next sync. Required env: `GOOGLE_CLIENT_ID/SECRET/CALENDAR_REDIRECT_URI`, `MICROSOFT_CLIENT_ID/SECRET/CALENDAR_REDIRECT_URI` (+ optional `MICROSOFT_TENANT_ID`, defaults `common`). Frontend `CalendarPage` now renders a real Outlook panel (no more "coming soon"). Drift allowlist no longer carries `/calendar/*` entries.
- T20 — `frontend/src/hooks/useAuthSync.jsx` exposes `<AuthProvider>` + `useAuth()`. Auth state lives in context (`{user, role, loading, refresh, setUser}`); `App.jsx` no longer keeps its own `useState(user)`. `/api/auth/me` is re-fetched on every route change, throttled to once per 5 min via a `lastFetchRef` (resets on cross-tab `storage` events). `handleImpersonate` and `exitImpersonation` call `refresh({force:true})` to bypass the throttle. Real-time push and tab-focus refresh are deliberately out of scope. Pages still using `safeReadJSON('user')` for read-only role checks (CalendarPage/TicketsPage/etc.) keep working because `setUser` mirrors to localStorage.
- Task #8 (2026-05-08) — Ported `/api/progress` discovery + roadmap surfaces from FastAPI to the worker (`cloudflare-worker/src/routes/progress.ts`, mounted at `/api/progress` in `index.ts`). Two new D1 tables added: `discovery_interviews` and `roadmap_okrs` — schema mirrored in `cloudflare-worker/src/schema.sql` and as a one-shot migration `cloudflare-worker/sql/migrations/001_progress_tables.sql` (NEW directory). Column names follow the FastAPI model (`interviewee_name`/`interviewee_role`/`interview_date`/`notes`/`hypotheses_json`/`pains_json`/`quarter`/`key_results_json`) — NOT the task brief's shorter proposal — because the shipping frontend (`DiscoveryPage.jsx`, `RoadmapPage.jsx`) already calls these endpoints with those field names and frontend changes are out of scope. Metrics sub-surface is explicitly out of scope (see follow-up task). A minimal `/signals/:projectId` IS implemented because DiscoveryPage refreshes call it via `Promise.all` alongside `listInterviews`; the interview-derived signals slider works fully off `discovery_interviews`, and the users/revenue sliders return 0 with `meta.reason: "no_metrics"` (same shape FastAPI returns when no snapshot exists) until the metrics table ships. Authorization mirrors `financials.ts` helpers (admin/partner/investor read; admin + owning founder write). `interview_logged` activity row uses hashed actor (T22.1). (Migration applied to remote D1 on 2026-05-08.)
- Task #2 (2026-05-08) — Documentation rewrite. `frontend/src/pages/DocsPage.jsx` is now a thin wrapper around `frontend/src/pages/docs/DocsLayout.jsx`. Content lives as data in `frontend/src/pages/docs/sections/*.js` (10 files: getting-started, spin-out-lab, build, validate-grow, capital, legal, network, portals, account, troubleshooting) + `index.js` manifest exporting `SECTIONS` and `buildSearchIndex()`. Each subsection has `{id, title, overview, howto[], tips[], related[]}`. Anchors are slash-format (`#section-id/subsection-id`) — DocsLayout uses `data-anchor` attributes + `querySelector` (not `getElementById`) because slash-containing ids would otherwise need `CSS.escape`. Client-side search uses `fuse.js` (newly added to `frontend/package.json`) over the flat manifest with weighted keys (subsectionTitle 0.5 / sectionTitle 0.2 / text 0.3) and snippet highlighting. All 89 related-feature links resolve to real anchors (verified). Account → "Settings overview" subsection mirrors the 9-tab structure shipped in Task #1 (Profile / Account / Security / Notifications / Privacy / Integrations / Billing / Appearance / Developer). Zero "github" or API/SDK developer references in copy (audience = end-users). `frontend/public/docs-screenshots/` directory created (empty); section data supports `screenshot` + `screenshotCaption` fields when assets are added later.
- Task #3 (2026-05-08) — Admin User Analytics tab on `/monitoring`. New sub-router `cloudflare-worker/src/routes/monitoring_analytics.ts` mounted at `/api/monitoring/analytics/*` (overview / cohorts / users / user/:id / financial / technical / export / audit / download/:token). Query + render helpers in `cloudflare-worker/src/services/analyticsReports.ts` (CSV + styled HTML). New table `admin_audit_log` (action='analytics_export') created idempotently at boot AND in migration `cloudflare-worker/sql/migrations/003_admin_analytics.sql` — applied to remote D1 on 2026-05-08 (alongside `001_progress_tables.sql` and `002_user_settings.sql`; `spinout_lab.sql` still pending). Frontend tab in new `frontend/src/pages/AnalyticsTab.jsx` (kept out of MonitoringPage.jsx to avoid bloating that 700-line file); MonitoringPage adds `analytics` to its tab list + `?tab=analytics` deep-link allowlist. Five sub-tabs: Overview / Users / Financial / Technical / Management. Default range last 30d, "View as anonymized" toggle masks emails/names but keeps IDs (pure frontend). **Pricing**: MRR/ARR derive from local `users.mi_subscription_*` columns (Stripe webhook keeps these in sync); plan→price map hardcoded in `analyticsReports.ts` `PLAN_MONTHLY_USD` (`mi_pro_monthly=$49`, `mi_pro_annual=$39`) — bump here if Stripe pricing changes. **Exports**: CSV always works; PDF tries `env.BROWSER` (Cloudflare Browser Rendering) first and **falls back to styled HTML when the binding is absent** — the response's `format` field reflects the actual artifact (`csv`/`pdf`/`html`) so the UI is never misled. Files saved to R2 (`FILES` binding) under `analytics-exports/<adminId>/<ts>-<rand>.<ext>`; download URL is a worker-served route `/api/monitoring/analytics/download/:token` gated by an HMAC token (24h TTL, signed off `JWT_SECRET`) — we deliberately don't issue real S3 presigned URLs because the worker has no S3 creds and a same-origin worker route avoids leaking R2 directly. When `FILES` is unbound, the export inlines a `data:` URL instead. Every export inserts a row into `admin_audit_log` (admin_user_id, report_type, format, filters_json, storage_key, download_url) which the "Recent Exports" panel reads via `/audit`. To enable real PDF: bind Cloudflare Browser Rendering as `BROWSER` in wrangler.toml and re-deploy — no code change needed.
- Task #3 (2026-05-09) — Due Diligence module. Routes at `/api/dd/*` (`cloudflare-worker/src/routes/dd.ts`) backed by `cloudflare-worker/src/services/dueDiligence.ts`. Migration `sql/migrations/008_due_diligence.sql` adds 8 tables (cases/sections/findings/external_sources/attachments/reviewers/reports/audit_log) — **pending apply to remote D1**. **PII at rest**: every free-text column that can carry PII or sensitive reviewer commentary is column-cipher v1 ciphertext via `services/columnCipher.ts` — `dd_cases.subject_email_enc`, `dd_cases.subject_legal_name_enc`, `dd_cases.notes_enc`, `dd_sections.reviewer_notes_enc`, `dd_findings.detail_enc/subject_name_enc/evidence_excerpt_enc`, `dd_external_sources.raw_response_enc`, and `dd_audit_log.details_enc` (audit details are JSON.stringify'd, encrypted, decrypted+JSON.parse'd in the `/audit` reader). Audit log actor uses hashed-email (T22.1). **R2 reports encrypted at rest** via `services/cryptoBox.encryptBytes` — DD report bytes are AES-GCM-encrypted before R2 put (key `…/<rand>.<ext>.enc`, content-type `application/octet-stream`); the row's `inner_content_type` + `encrypted=1` columns let `/reports/download/:token` decrypt and stream the original MIME with `Cache-Control: private, no-store`. **Connectors** (8: opencorporates / sec_edgar / sanctions_ofac / newsapi / gdelt / linkedin / github / whois_dns) are feature-flagged stubs (`DD_FLAG_<KEY>`); deterministic findings keyed off subject label. **Risk score**: section weighted-mean, capped per-section by worst unresolved severity OR overridden by reviewer verdict; bands ≥0.8 green / ≥0.6 yellow / ≥0.4 amber / else red. **Async scan**: `/cases/:uid/scan` inserts `dd_external_sources` rows as `queued`, returns 202, runs each connector in `executionCtx.waitUntil` → `processConnector` transitions queued→running→ok/error; UI polls case GET while any source is in-flight. **PDF**: `renderReportArtifact` prefers Cloudflare Browser Rendering binding, falls back to **pdf-lib** (`renderReportPdf`, Axal-VC-branded layout in `services/dueDiligence.ts`), HTML only on pdf-lib failure — `format` always reflects the actual artifact. Stored to R2 under `dd-reports/<caseId>/...`; download via worker route gated by `mintDownloadToken` (5-min TTL, single-use). **Reviewer magic-link**: `/assign` mints `dd_reviewers.magic_link_jti` + emails link `…?section=<id>&inv=<jti>`; new `POST /cases/:uid/reviewer-invite/:jti` validates jti against the calling user, records `responded_at`, audits `reviewer_invite_accepted`. Frontend strips `?inv=` from URL after acceptance and scrolls/highlights the focused section. **NDA pipeline**: `POST /cases/:uid/sections/:sectionId/nda` accepts multipart upload → R2 (`dd-ndas/<caseId>/<sectionId>/...`) + `dd_attachments` row + `nda_signed_at` on both section + reviewer rows. `/verdict` now ENFORCES presence of an attachment uploaded by the calling user before accepting any verdict other than `n_a` (returns 412); the legacy `body.nda_signed` boolean is ignored. **Authorization**: `requireDdReader` blocks every role outside admin/partner/investor/mentor (founders never read); `requireCaseWriter` additionally requires owner OR `dd_reviewers` row on the case for every partner/investor/mentor mutation (scan/assign/recompute/report). Admins are unscoped. NDA upload route additionally validates that the URL `sectionId` belongs to the URL `caseId` (closes a cross-case URL-tampering bug). **Audit drawer**: `/cases/:uid/audit` RBAC = admin OR owner OR any reviewer on the case; UI slide-over panel renders an evidence/event timeline in `AdminDueDiligenceCasePage.jsx`. **Expertise routing**: `GET /dd/experts?section_key=…` returns `suggestions` (users with the most prior completed verdicts on the same `section_key`, ranked by count) + `eligible` (all admin/partner/mentor/investor users); the AssignModal renders suggested experts as one-click cards with prior-review counts before the full eligible-user dropdown. **Case dashboard**: detail page now also renders a finding heatmap (sections × severity grid) and a reviewer queue table (NDA / accept / verdict status per assignee). Frontend: list page + detail page; `dd` namespace in `lib/api.js` (incl. `acceptInvite`, `uploadNda`, `experts`). Email path: existing `services/notify.ts` → `services/email.ts` (noreply@axal.vc Gmail OAuth) — `GMAIL_*` secrets missing in dev so notify call swallows the failure.
- Task #1 (2026-05-10) — Slack notifications integration (BETA, free tier). Provider `cloudflare-worker/src/integrations/providers/slack.ts` (side-effect imported from `index.ts`). One-way Worker→Slack via incoming-webhook OAuth (scope: `incoming-webhook` only — user picks the channel during the Slack consent screen, no `channels:read` needed). Registry entry flipped `coming_soon`→`beta`, capabilities updated to `Channel notifications / Block Kit messages / Quiet-hours aware` (dropped DM digests + slash commands — out of scope, no bot token). Required env secrets per env: `SLACK_CLIENT_ID`, `SLACK_CLIENT_SECRET`. Migration `cloudflare-worker/sql/migrations/022_slack_notif_categories.sql` adds `user_settings.notif_categories_slack TEXT NOT NULL DEFAULT '{}'` — **pending apply to remote D1**. Idempotent `ALTER TABLE ... ADD COLUMN` also added to `services/userSettings.ts:ensureUserSettings` so dev D1s lazily get the column. **notify.ts rewrite**: removed the legacy global `env.SLACK_WEBHOOK_URL` (was a privacy footgun — broadcast every user's notifications to one shared channel) in favor of `loadSlackWebhookForUser(env, userId)` which reads the per-user webhook from the `integrations` row (`provider_key='slack' AND status='active'`); decrypts via `decryptCredentials`. Block Kit renderer (`buildSlackBlocks`) emits header + section + actions (link button) + context blocks for the 5 spec'd events: `contract_signed`, `deal_stage_change`, `mentor_session_booked`, `score_generated`, `dd_report_ready`. Quiet hours suppress Slack same as email; critical categories bypass. Per-event opt-in map (`notif_categories_slack`) checked when `args.category` is set — **default-on**, only explicit `false` suppresses. **5 trigger sites wired**: `routes/deals.ts` (deal_stage_change — added 'slack' + category), `routes/scoring.ts` (score_generated — added 'slack' + category), `routes/dd.ts` (dd_report_ready — added 'slack' + category), `routes/esign.ts` (NEW notify after `allSigned` block — fires `contract_signed` to envelope owner), `routes/mentors.ts` (NEW notify after booking insert — fires `mentor_session_booked` to mentor's user_id). All 5 are best-effort try/catch — never block the originating API. **Settings route** `routes/settings.ts:pickNotifications` + PUT handler now read/write `notif_categories_slack`. **Disconnect**: provider's `disconnect()` clears `user_settings.notif_categories_slack='{}'` so a re-connect starts from defaults; webhook itself is wiped when the route layer deletes the integrations row. Slack provides no first-class revoke endpoint for incoming-webhook URLs (admins remove the app from the workspace). **Frontend**: `SettingsPage.jsx` `NotificationsSection` polls `api.integrationsList()` once on mount, gates the Slack column to `disabled=true` with an inline "Connect Slack" link to `/settings/integrations` when no active Slack integration exists (fail-closed — never invite the user to toggle a channel that won't deliver). Replaced stale "Coming soon" label on the Slack row in `IntegrationsTab` with "Connect from the Integrations marketplace". `npm run test:drift` clean; worker `tsc --noEmit` clean; frontend `vite build` passes.
- Task #2 (2026-05-10) — DocuSign e-sign integration (Live, Studio tier). New provider `cloudflare-worker/src/integrations/providers/docusign.ts` (OAuth2 Auth Code; scopes `signature extended`; auth host `account-d.docusign.com` for demo / `account.docusign.com` for prod, switched via `oauth_state_tokens.extra_json.is_demo` and persisted on the integration row's `config_json.is_demo`). Registry entry flipped from `coming_soon`/`oauth2` → `live`/`oauth2`. Side-effect import + hourly cron sweep (`syncAllDocusignIntegrations`) wired in `index.ts` (gate `minute===5` to spread load away from top-of-hour batches). Required env secrets: `DOCUSIGN_CLIENT_ID`, `DOCUSIGN_CLIENT_SECRET` (per env). Migration `cloudflare-worker/sql/migrations/021_docusign_provider.sql` adds two columns to existing `esign_envelopes` table: `provider TEXT NOT NULL DEFAULT 'native'` + `docusign_envelope_id TEXT` + indexes — **pending apply to remote D1**. ALTERs are also added (idempotently, swallowing duplicate-column errors) to `routes/esign.ts:ensureSchema` so dev D1s lazily get the columns on first request. **Send path**: `createAndSendEnvelope` accepts new optional `viaProvider:'native'|'docusign'` opt; when 'docusign' AND the admin has an active DocuSign integration, we render the agreement to PDF (via existing `services/pdf.ts:renderAgreementPdf` with a 1×1 transparent-PNG signature placeholder), POST to `/restapi/v2.1/accounts/<accountId>/envelopes` with a `signHereTabs` anchor on the literal string `_____ESIGN_HERE_____` we suffix to the body, persist `provider='docusign'` + `docusign_envelope_id`, and skip the in-house token/email path (DocuSign emails the signer). On any failure we fall back to the in-house flow. New API field `via_provider` accepted on `POST /api/legal/esign/send`. **Webhook (DocuSign Connect)**: arrives at the generic `/api/integrations/webhook/docusign/<uid>`. Custom `verifyWebhook` reads `X-DocuSign-Signature-1..5` headers (base64-encoded HMAC-SHA256, multi-key support), constant-time-compares against the per-connection `webhook_secret`. Body parser handles both JSON (`data.envelopeId`) and legacy XML (`<EnvelopeID>`). Both webhook + cron call `reconcileEnvelope` which is idempotent — pulls envelope status, on `completed` fetches `/documents/combined` and stores VERBATIM (not re-encrypted; matches in-house flow's R2-at-rest model) at `esign/signed/<envelope_uuid>.pdf` so existing download routes work without a provider switch. Also flips all non-rejected `esign_recipients` rows to `signed` and appends a `docusign_<status>` row to `esign_audit_events`. **Token mgmt**: standard refresh-token flow with KV-based lock (`docusign:refresh:<uid>`, 30s TTL) to prevent thundering herd; `getActiveAccessToken` proactively refreshes within 60s of expiry; `dsFetch` retries once on 401. **Disconnect** best-effort calls `/oauth/revoke`. **Frontend**: `AdminPage.jsx` ContractRow now renders an amber "DocuSign" badge (replacing the green "eSign" badge) when `c.provider === 'docusign'`. `UnifiedContract` type + `loadEsignContracts` SQL + `enrichEsignRow` extended with `provider` field (`COALESCE(e.provider, 'native')`). `frontend/src/lib/api.js:esignSend` already accepts arbitrary body fields so no API helper change needed. **Out of scope**: auto-provisioning of DocuSign Connect at the account level (per-envelope `eventNotification` covers the common case; admin-wide Connect subscription is follow-up #5). `npm run test:drift` clean; worker `tsc --noEmit` clean; frontend `vite build` passes. **Hardening rev (same day, post-review)**: (1) signed PDFs now stored AES-GCM-encrypted via `cryptoBox.encryptBytes` at `esign/signed/<uuid>.pdf.enc`; both download routes (`GET /sign/:token/document` + `GET /:id/document`) decode via new `materializeSignedPdf` helper that branches on the `.enc` suffix. (2) Send-envelope payload now embeds DocuSign `eventNotification` with a per-integration HMAC secret (lazily generated by new `ensureDocusignWebhookSecret`, persisted to `integrations.webhook_secret_enc` via `encryptWebhookSecret`); means the webhook works without admin-side Connect config. (3) Migration 021 + ensureSchema now also create partial UNIQUE index `uq_esign_user_docusign_envelope ON (user_id, docusign_envelope_id) WHERE docusign_envelope_id IS NOT NULL` — guarantees SQL-level idempotency between webhook + cron + retried sends. (4) `routes/profiling.ts` verify endpoint now accepts + forwards `via_provider` (from already-parsed body — fixed body-double-read bug). (5) `routes/admin_contracts.ts` GET / accepts `?provider=native|docusign` filter; `AdminPage.jsx` ContractsPanel renders an All/Native/DocuSign filter chip + adds the provider toggle to the verify modal (with Studio-tier upsell variant for non-eligible plans).
- Task #5 (2026-05-10) — Carta integration (Live, Studio tier). New provider `cloudflare-worker/src/integrations/providers/carta.ts` (OAuth2 + PKCE; scopes `read:cap_table read:stakeholders read:securities`; auth host `login.carta.com`, API base `api.carta.com/v1alpha1`). Registry entry flipped from `coming_soon`/`api_key` → `live`/`oauth2`. Side-effect import + 6h cron sweep (`syncAllCartaIntegrations`) wired in `index.ts` (gate `hour%6==0 && minute==0`). Provider also implements `postConnect` so the OAuth callback path runs an immediate first sync via `executionCtx.waitUntil` — no 6-hour dead period before the first cap table appears. Required env secrets: `CARTA_CLIENT_ID`, `CARTA_CLIENT_SECRET` (per env). Migration `cloudflare-worker/sql/migrations/020_carta_integration.sql` adds two NEW tables `cap_table_holders` + `cap_table_securities` (separate from the simulator's `cap_table_scenarios`), each with a `source` column ('manual'|'carta') and Carta-id columns (`carta_stakeholder_id`/`carta_security_id` and `carta_id` respectively) under partial UNIQUE indexes (only when carta-id IS NOT NULL). **Pending apply to remote D1.** Sync flow upserts holders × securities pairs (one row per stakeholder per holding), and reconciles by flipping rows that disappeared from upstream back to `source='manual'` (never deletes). Disconnect mirrors that semantic — bulk `UPDATE … SET source='manual'` on both tables for the user, then best-effort `POST /o/revoke_token/`. New read endpoint `GET /api/captable/live` returns `{connected, issuer_name, last_synced_at, last_error, holders[], securities[], total_shares}`; ownership_pct is recomputed off the live total so manual + Carta rows share a denominator. Frontend `CapTablePage.jsx` gains a `LiveCapTablePanel` component above the simulator: orange banner "Synced from Carta — last sync N min ago", per-row Carta logo (Lucide PieChart tinted orange) + lock icon + read-only "Carta" badge with tooltip, manual rows badged "Manual". Refresh button + "Manage" link to `/integrations`. When user has never connected and has no rows the panel collapses to a slim dashed-border "Connect Carta" upsell. Out of scope (deliberate): write-back to Carta, Pulley provider, 409A artefact pulls. `npm run test:drift` clean; worker `tsc` + frontend `vite build` both pass.
- Task #1 (2026-05-08) — Settings expansion (tabbed). `frontend/src/pages/SettingsPage.jsx` is now a 9-tab page (Profile / Account / Security / Notifications / Privacy / Integrations / Billing / Appearance / Developer). Backend was 100% already in place from prior **Task #20 — Phase B · Prompt 6**: migration `cloudflare-worker/sql/migrations/002_user_settings.sql` (user_settings table), service `cloudflare-worker/src/services/userSettings.ts` (CRUD + `isInQuietHours`), all sub-routes in `cloudflare-worker/src/routes/settings.ts` (`/v2`, `/profile`, `/security`, `/notifications`, `/privacy`, `/appearance`, `/integrations`, `/developer`), and `frontend/src/contexts/SettingsContext.jsx` applying theme/density/sidebar globally via `<html data-theme/data-density>` (live OS-theme follow on `theme=system`). Public Directory exclusion (`show_in_directory=0`) is wired in both `partnernet.ts:404` and `cofounder.ts:384`; quiet-hours skip is wired in `services/notify.ts:124`. Back-compat URL aliases preserved: `/settings/jurisdictions→profile`, `/email→account`, `/auth→security`, `/role→profile`. Role gating: Billing tab visible to founders, Developer tab to admins only. Developer tab's "API keys" UI is gated behind a server-driven feature flag (`api_keys_enabled` in the integrations payload) per user pref. Migration applied to remote D1 on 2026-05-08 (alongside `001_progress_tables.sql` and `003_admin_analytics.sql`; `spinout_lab.sql` remains pending). All new tab components live inline at the bottom of `SettingsPage.jsx` (single-file by design — keeps Task #1 to one frontend file; if it grows further, follow-up: split into `frontend/src/pages/settings/`).
- Task #3 (2026-05-10) — Calendly integration (Live, Free tier). Provider module `cloudflare-worker/src/integrations/providers/calendly.ts` (side-effect imported from `index.ts`) supports BOTH OAuth2 authorization-code AND Personal Access Token (PAT); both validated against `/users/me`, both stored as a unified credential blob with `auth_method: 'oauth' | 'pat'` (PAT path has no refresh_token / expires_at). OAuth refresh uses the same KV-leased single-flight pattern as HubSpot (`calendly:refresh:<uid>`, 30 s TTL). Webhook subscription is provisioned **lazily on the first `sync()` run** (not at connect — connect runs before the integration row exists, so the callback URL `/api/integrations/webhook/calendly/<uid>` cannot be built yet); the per-subscription `signing_key` Calendly returns is encrypted into `webhook_secret_enc` and the generic webhook receiver in `routes/integrations.ts` HMAC-verifies inbound bodies against it. Events are projected into a new unified table `calendar_events` (migration `018_calendly_integration.sql` — **PENDING apply to remote D1**; also declared inline in `cloudflare-worker/sql/schema.sql` for fresh-bootstrap parity); UNIQUE(source, external_uri) makes both webhook + cron upserts idempotent. `cloudflare-worker/src/services/calendar.ts` adds `'calendly_event'` to the `CalendarEvent.kind` union + a new `calendlyEvents()` reader; default kinds set in `fetchUserEvents` includes it. `index.ts` cron adds a 15-minute reconcile branch (`now.getUTCMinutes() % 15 === 0`) calling `syncAllCalendlyIntegrations(env)` — safety-net for portals where webhooks are dropped. **Sync window**: -24h..+60d (front-end of the window heals webhook-missed past events, back-end populates upcoming bookings). `disconnect` revokes the webhook subscription (best-effort) and DELETEs all projected `calendar_events` rows for that user — no OAuth revoke endpoint exists on Calendly's side; users revoke from Calendly account settings. `validateConfig` allowlists `booking_url`, `default_event_type_uri`, `default_event_type_name`. `action('list_event_types')` powers a future event-type picker. **Required prod secrets**: `CALENDLY_CLIENT_ID`, `CALENDLY_CLIENT_SECRET` — provision with `npx wrangler secret put CALENDLY_CLIENT_ID --env=production`. Frontend: `IntegrationsPage` ConnectModal extended with `provider.supports_pat` flag (added to `ProviderDescriptor` and the public registry payload) — when set, OAuth providers also render a Personal Access Token field; submit button enables when EITHER OAuth completes OR a PAT is pasted. `CalendarPage` adds a `Calendly` filter pill + `bg-sky-100` badge styling + `KIND_LABEL`. Worker `tsc --noEmit` and frontend `vite build` both green; `npm run test:drift` clean (no new mounts — webhook callback rides the existing `/api/integrations/webhook/:provider/:uid` surface). **Post-review fixes (3 severe issues from architect):** (a) `ProviderImpl` extended with optional `verifyWebhook(secret, body, headers)` — Calendly's `Calendly-Webhook-Signature` is Stripe-style `t=<unix>,v1=<hex_hmac>` over `<t>.<body>`, NOT the default `sha256=<hex>` over body, so the central verifier in `routes/integrations.ts` would have rejected every legitimate Calendly webhook; provider-specific verifier overrides the default when present, with a 5-minute replay window. (b) `ensureWebhookSubscription` now KV-lock single-flighted (`calendly:wh_provision:<uid>`, 60 s TTL) AND re-reads row state under the lock — concurrent syncs/cron ticks can no longer create duplicate Calendly webhook subscriptions (which would otherwise deliver each event twice). (c) `ProviderImpl` extended with optional `postConnect(c, user, row)` hook the routes layer fires via `executionCtx.waitUntil` after both the JSON `/connect` AND the OAuth callback persist a row — Calendly's `postConnect` immediately provisions the webhook + runs a first sync, eliminating the otherwise-15-minute "dead period" between connect and the first booking appearing.
- Task #2 (2026-05-10) — HubSpot integration (Live, Growth tier). Provider module `cloudflare-worker/src/integrations/providers/hubspot.ts` (side-effect imported from `index.ts`) implements OAuth code-exchange (no PKCE — HubSpot doesn't support it; signed state still binds the callback), bearer access tokens with refresh-on-expiry, two-way deal sync, project→company push, deal.propertyChange + contact.creation webhook, disconnect-via-revoke. Stage map at `integrations/mappings/hubspot_stages.ts` (default Sales Pipeline IDs; per-row override via `config.dealstage_map`). Migration `017_hubspot_integration.sql` adds `deals.hubspot_deal_id`, `projects.hubspot_company_id`, `projects.hubspot_primary_contact_id` + indexes — **pending apply to remote D1**. Schema columns also declared inline on the `projects` and `deals` CREATE TABLE blocks in `cloudflare-worker/sql/schema.sql` for fresh-bootstrap parity. Generic registry surface extended: `ProviderImpl.action(c,user,row,name,body)` + `GET|POST /api/integrations/:uid/action/:name` and `PATCH /api/integrations/:uid/config` (tier-gated, owner+admin only, logs `config_update` event). HubSpot exposes `action('list_pipelines')` for the picker. Cron 30-min reconcile branch (`now.getUTCMinutes() % 30 === 0`) calls `syncAllHubspotIntegrations(env)`. Post-write hooks: `routes/deals.ts` PUT pushes deal stage to HubSpot via `c.executionCtx.waitUntil` after status update; `routes/projects.ts` createProjectHandler creates HubSpot company + primary contact same way. Both fire-and-forget; failures land in `integration_logs`. **Token refresh race fix**: `getActiveAccessToken` uses a 30-s KV lease keyed `hubspot:refresh:<uid>` so concurrent isolates don't burn HubSpot's single-use refresh token; losers spin-poll D1 (≤3 s) for the winner's persisted blob. **Webhook fail-closed**: signature verification requires `HUBSPOT_CLIENT_SECRET` + `x-hubspot-request-timestamp` + `x-hubspot-signature-v3` (timestamp+method+url+body HMAC-SHA256 base64); the route-supplied `signature` arg is intentionally ignored. **Required prod secrets**: `HUBSPOT_CLIENT_ID`, `HUBSPOT_CLIENT_SECRET` — provision with `npx wrangler secret put HUBSPOT_CLIENT_ID --env=production`. Frontend `IntegrationsPage` adds "Pipeline" button + `HubspotConfigModal` (pipeline select + per-stage HubSpot dealstage mapping, persists via `PATCH /:uid/config`); connected card shows `portal #<hub_id>` + `pipeline: <label>` when set. New api helpers: `api.integrationsAction(uid, name, body?)` + `api.integrationsPatchConfig(uid, patch)`. Worker `tsc --noEmit` and frontend `vite build` both green; `npm run test:drift` clean.
- Task #14 (2026-05-10) — Notifications digest + quiet hours. New table `notification_outbox` (`cloudflare-worker/sql/migrations/013_notifications_digest.sql` — **pending apply to remote D1**) buffers non-critical email when the user is in quiet hours OR digest is daily/weekly. `services/notify.ts` adds an optional `category` arg + `CRITICAL_CATEGORIES = {security, billing, contract_sign_request}`; uncategorised callers are treated as critical (no behavioural change for the 10 existing notify call-sites). Routing rules per channel: in-app inbox always written; realtime push skipped during quiet hours for non-critical; email = critical→send, in-quiet+digest=off→`activity_logs.suppressed_quiet_hours`+drop, in-quiet+digest=daily|weekly→outbox, !quiet+digest=daily|weekly→outbox, otherwise→send. Slack is intentionally NOT digestable for this slice (out of scope per spec). `flushPendingDigests(env)` runs on every cron tick (existing `* * * * *` schedule); per-user GROUP BY scan is cheap and only sends when the user's local time is `09:00` with a matching weekday (`weekly`→Mon). Digest cap = 100 rows / user / send. **Validation**: `SettingsValidationError` now carries a `field` property; `routes/settings.ts:handleSettingsError` returns `{error, field, errors:{[field]:msg}}` so the form can inline messages instead of toasting. tz validated via `Intl.supportedValuesOf('timeZone')` with a runtime probe fallback. Frontend tz input is now a `<select>` (defaults to the user's profile timezone) and inline `fieldErrors` render under each affected input. Settings columns (`digest_frequency`, `quiet_hours_*`) were already created by `002_user_settings.sql` so 013 only adds the outbox table.
- Task #13 (2026-05-10) — Admin User Analytics tab (`/admin/monitoring?tab=analytics`) hardened end-to-end. **Frontend** (`frontend/src/pages/AnalyticsTab.jsx`): URL-persisted range picker (`?from=&to=`, default 30d), `RetryCard` inline empty/error UI per sub-tab (never error toasts), `EmptyPill` for clean empty states, all 5 sub-tabs (Overview/Users/Financial/Technical/Management) + UserDrillDown + RecentExports use `_analyticsRead` (1 retry on 5xx/network) and honor `data.meta.reason==='no_data'`. **Backend** (`cloudflare-worker/src/services/analyticsReports.ts` + `routes/monitoring_analytics.ts`): added `meta.reason` to Overview/Financial/Technical reports; **snapshot read path** — `loadOverviewFromSnapshots()` reads pre-computed daily rollups from `analytics_snapshots` for fully-historical windows (returns null when range includes today or coverage incomplete → falls back to live aggregate); `loadOverview()` accepts `{forceLive:true}` to bypass snapshots — used by `writeDailySnapshot()`/`backfillSnapshots()` so rebuilds always recompute from source rows (otherwise corrections never propagate). **Workers Analytics Engine integration** — `wrangler.toml` declares `[[analytics_engine_datasets]] binding=ANALYTICS dataset=studioos_metrics`, `middleware/observability.ts` writes a data point per request with required dimensions `{route, method, role, status, tier}` in blobs and `{latency_ms, status, user_id}` in doubles (tier resolved opportunistically via `users.mi_subscription_plan` lookup, only when binding is configured + user is known); best-effort, never throws. `loadTechnicalFromAnalyticsEngine()` reads via Cloudflare's SQL API at `api.cloudflare.com/.../analytics_engine/sql` gated by `CLOUDFLARE_ACCOUNT_ID` + `CLOUDFLARE_AE_API_TOKEN` (falls back to D1 `system_metrics` when unconfigured/error). **GET /api/monitoring/analytics/management** — single server-composed call returning `{overview, financial, technical}` (replaces 2 client-side round-trips). **PDF→HTML fallback** — when Browser Rendering binding is missing or fails, `/export` returns HTML artifact with `format: 'html'` instead of 503 (so exports never error). **POST /api/monitoring/analytics/snapshots/backfill** — admin-gated, clamps `days` to 1..90, calls `backfillSnapshots()`. Daily snapshot cron at 02:05 UTC writes to `analytics_snapshots` (migration `cloudflare-worker/sql/migrations/012_analytics_snapshots.sql` — **pending apply to remote D1**). New env (optional, both required to enable AE reads): `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_AE_API_TOKEN` (Workers Analytics Read scope). Worker `tsc --noEmit` + frontend `vite build` both green.
- Task #6 (2026-05-10) — SMS 2FA via GCIP. **Deploy-time remediation**: `services/totpRemediation.ts:runTotpRemediation()` migrates every legacy base32 secret out of `users.password_hash`, sets `password_reset_required=1`, and emails each user a forced-reset link via `sendVerificationEmail`. Trigger surfaces: (a) admin `POST /api/admin/maintenance/totp-remediation` (operators run after deploy — TOTP step-up gated), (b) daily 04:20 UTC cron in `index.ts` `scheduled()` as backstop. Idempotent (filters by BASE32 regex + missing auth_totp row), paginated (200/batch, max 25 passes per invocation), per-user try/catch. **GCIP disable**: `services/gcip.ts:deleteGcipPhone()` POSTs `{localId, deleteProvider:["phone"]}` to `identitytoolkit.googleapis.com/v1/projects/{GCIP_PROJECT_ID}/accounts:update` with `Authorization: Bearer ${GCIP_ADMIN_BEARER_TOKEN}`. `/sms/disable` calls upstream FIRST and returns 502 if it fails (so admin can retry); only `admin_unconfigured` / `no_local_id` fall through to local-only wipe. New env: `GCIP_PROJECT_ID`, `GCIP_ADMIN_BEARER_TOKEN` (service-account OAuth2 access token, ~1h TTL — refresh externally; auto-refresh inside the worker is out of scope).
- Task #6 (2026-05-10) — SMS 2FA via Google Cloud Identity Platform (Firebase Phone Auth). Migration `cloudflare-worker/sql/migrations/011_sms_2fa.sql` adds `auth_sms` (encrypted phone via columnCipher v1, `phone_country`, `last4`, `gcip_local_id`), `users.tfa_methods` (JSON CSV of enrolled factors) and `user_sessions.factor` (`'totp'|'sms'|'recovery'`) — **pending apply to remote D1**. New service `services/authSms.ts` (encrypt/decrypt, `isCountryAllowed` against `SMS_COUNTRY_ALLOWLIST` env CSV — defaults to a 27-country allow-list, `*` opts everyone in). New service `services/gcip.ts` calls `identitytoolkit.googleapis.com` REST (sendVerificationCode + signInWithPhoneNumber); `isGcipConfigured()` gates every route — `/auth/sms/*` returns 503 when `GCIP_API_KEY` is absent so single-tenant deploys without GCIP just hide the SMS UI. Routes in `routes/auth_sms.ts` mounted via `auth.route('/', smsRoutes)` in `routes/auth.ts` (so `/api/auth/factors`, `/api/auth/sms/*` stay on the same prefix): `/factors` (unauth, 30/min/IP, returns same shape on miss to avoid enumeration), `/sms/start-enrollment` + `/sms/confirm-enrollment` (auth, 10/min/IP + 5/min/user, persists encrypted phone), `/sms/disable` (refuses with 412 if TOTP not configured — every account must keep ≥1 factor), `/sms/start-challenge` + `/sms/verify-challenge` (login flow; sessionInfo bound to (user_id,email) in KV against replay; mints JWT with `factor='sms'` on `user_sessions.factor`), `/sms/status` (auth, last4 + country only). `requireFactor(c, 'totp')` middleware in `auth.ts` reads the active session row's `factor` column; missing column / older session → fail-open (back-compat) so existing TOTP sessions aren't locked out. **HIGH-RISK ROUTES gated TOTP-only** via `requireFactor(c, 'totp')`: `admin.impersonate`, `billing.checkout`, `billing.portal`, `admin_contracts.void`, `dd.cases/:uid/report`. Recovery-code logins record `factor='recovery'` (NOT `'totp'`) — a printed recovery code can sign in but cannot escalate to billing/impersonation. `setup-totp` + settings `/totp/repair` both call `setUserFactor(env, uid, 'totp')` so `tfa_methods` stays in lock-step with the auth_totp row. Settings `/security` GET now returns `sms_configured`, `sms_last4`, `sms_country`, `tfa_methods`. Frontend: `LoginPage.jsx` is now a 3-step picker (email → /factors → choose TOTP/SMS → verify), TOTP pre-selected when both are enrolled. SettingsPage Security tab adds a `<SmsPanel>` (country picker + E.164 phone input + SIM-swap warning banner; disable button gated on TOTP presence; last-4 display only — full phone is never sent to the client). `frontend/src/lib/api.js` adds `authFactors`, `smsStatus`, `smsStartEnrollment`, `smsConfirmEnrollment`, `smsDisable`, `smsStartChallenge`, `smsVerifyChallenge`. Required env (prod-only when SMS is desired): `GCIP_API_KEY`, optional `GCIP_RECAPTCHA_TOKEN_FALLBACK`, optional `SMS_COUNTRY_ALLOWLIST`. FastAPI dev backend has a known TOTP-storage bug at `backend/app/api/routes/auth.py:492` (left as-is per existing gotcha — dev-only, never deployed; worker side is correct via auth_totp from Task #1/#33).
- Task #5 (2026-05-10) — Dashboard personal assistant chatbot. New worker route `cloudflare-worker/src/routes/assistant.ts` mounted at `/api/assistant`. Migration `sql/migrations/010_assistant.sql` adds `assistant_conversations` + `assistant_messages` + `assistant_feedback` tables and a `users.assistant_enabled` column — **pending apply to remote D1**. Anthropic SDK is NOT used; we call `https://api.anthropic.com/v1/messages` directly with `stream:true` on every hop. **Streaming**: every hop streams (including tool-use hops via `input_json_delta` events). The SSE parser handles multi-line frames (`event: ...\ndata: ...`) and concatenates `data:` lines per spec — an early version that gated on `chunk.startsWith('data:')` silently dropped most events. **Tool loop**: max 3 iterations; tools = `listAvailableFeatures`/`deepLink`/`recentActivity`/`pendingContracts`/`upcomingMeetings`/`scoringSummary`. Tool output is sanitised (control chars stripped, `<tool_use|tool_result>` tags neutered, 6k cap) and wrapped in `<tool_result>...</tool_result>` to neutralise prompt injection from DB content. **Cost accounting**: each hop's row stores ONLY that hop's tokens/cost in `assistant_messages.cost_usd_micros`; the conversation rollup is the SUM. Analytics `loadAssistantCost()` in `services/analyticsReports.ts` (surfaced in `FinancialReport.assistant_cost`) sums message rows by model — never double-counts multi-hop runs. **message_count** is recomputed via `(SELECT COUNT(*)…)` per turn rather than `+=2` because hop counts vary. **Model routing**: `claude-haiku-4-5-20251001` default, `claude-sonnet-4-6` for admins or messages >4000 chars. **Retention**: daily 04:10 UTC cron `sweepExpiredConversations` (90d free / 1y paid / 5y admin opt-in via `users.assistant_retain_history=1`). **System prompt** is `cache_control: ephemeral` for Anthropic prompt caching. Onboarding `/complete` flips `users.assistant_enabled=1` (best-effort, swallows missing-column errors so older DBs aren't blocked). **Frontend**: floating launcher + chat panel in `frontend/src/components/PersonalAssistant.jsx` mounted on Dashboard; consumes SSE via `fetch` + `ReadableStream` (NOT EventSource — needs auth headers). Markdown-lite render: paragraphs, **bold**, `- ` lists, and `[label](url)` links — links starting with `/` become react-router `<Link>` deep-link buttons, others open in new tab. Conversation list/rename/delete UI; thumbs feedback per-message. Hidden in print via `print:hidden`. Settings → Notifications adds `proactive_nudges` row in NOTIF_CATEGORY_KEYS (stored in existing `notif_categories_inapp` JSON; no schema change). Activity logs use hashed actor (T22.1). `ANTHROPIC_API_KEY` is required — when absent, the route returns 503 and the launcher still renders harmlessly. Out of scope: destructive tool actions, voice. (Currently mounted only on Dashboard — see follow-up to mount globally.)
- Task #4 (2026-05-10) — Investor Signals + profiling chatbot. New worker route `cloudflare-worker/src/routes/investor_signals.ts` mounted at BOTH `/api/investor-profile` and `/api/investor-signals` (single Hono app, two prefixes — `profile/me` and `signals/latest` paths disambiguate). Migration `sql/migrations/009_investor_signals.sql` adds `investor_profiles` (per-user chatbot answers + `contribute_to_signals` flag) and `investor_signals_snapshots` (k-anonymized aggregates, payload_json) — **pending apply to remote D1**. Aggregator `aggregateInvestorSignals(env)` runs from the existing cron in `index.ts` every 6h at HH:05 (00:05/06:05/12:05/18:05 UTC); only profiles with `contribute_to_signals=1` AND `users.role='investor'` AND `completed_at IS NOT NULL` are counted. **k-anonymity**: `MIN_CELL_SIZE=5`; cells below threshold render `{n: null, reason: 'insufficient_data'}` and ticket median/IQR returns `insufficient_data` until n_total≥5. Snapshot history capped at 200 rows (~50d at 6h cadence). Frontend: `OnboardingInvestorPage.jsx` extended from 4 → 7 wizard steps (added geo, thesis, contribute toggle); on finish, `api.saveInvestorProfile()` mirrors answers to the profile endpoint (failure swallowed — soft enrichment, never blocks onboarding). New "Axal Investor Signals" tab in `MarketIntelPage.jsx` renders sector/stage/geo/ticket bars, thesis cloud, ticket median+IQR KPIs and 30d participation trend. Privacy toggle lives in `SettingsPage.jsx` `InvestorSignalsContributionCard` — flips `contribute_to_signals` via the same PUT endpoint; opting out removes the user from the next snapshot within 6h. One-time existing-investor reprompt is the violet banner inside the Investor Signals tab when role=investor && profile.completed_at is null.
- T5 — TOTP recovery codes are stored as SHA-256 hex hashes in the existing `users.totp_recovery_codes` JSON column (added in `epic3_settings.sql`); we deliberately did NOT add a separate `totp_backup_codes` table because the column-based design already shipped (Settings UI + regenerate endpoint) and a single-row UPDATE gives atomic single-use semantics. Generation paths: `/api/auth/setup-totp` mints 10 codes at enrol (returned once in `recovery_codes`); `/api/settings/totp/recovery-codes/regenerate` mints 10 in Settings. Consumption: `/api/auth/login` falls back to `tryConsumeRecoveryCode` when the 6-digit TOTP is invalid (logs `user_login_recovery_code` in activity_logs). `/api/auth/verify-totp` does NOT consume codes (it's a setup-time confirmation only). `/api/auth/verify-email` is now IP-rate-limited 10/15min via `verify-email-ip:` KV key. `/login`, `/verify-totp`, `/resend-verification` per-email caps were already in place.

## Pointers
- **Cloudflare Workers Docs**: [https://developers.cloudflare.com/workers/](https://developers.cloudflare.com/workers/)
- **Hono Docs**: [https://hono.dev/](https://hono.dev/)
- **React Docs**: [https://react.dev/](https://react.dev/)
- **Tailwind CSS Docs**: [https://tailwindcss.com/docs](https://tailwindcss.com/docs)
- **Vite Docs**: [https://vitejs.dev/](https://vitejs.dev/)
- **FastAPI Docs**: [https://fastapi.tiangolo.com/](https://fastapi.tiangolo.com/)
- **Cloudflare D1 Docs**: [https://developers.cloudflare.com/d1/](https://developers.cloudflare.com/d1/)
- **`CLAUDE.md`**: For full architecture rules and detailed context (if available).
- Task #4 (2026-05-10) — Salesforce integration (Studio tier, live). Provider at `cloudflare-worker/src/integrations/providers/salesforce.ts`; field/stage maps in `cloudflare-worker/src/integrations/mappings/salesforce_fields.ts`; registry entry flipped `coming_soon`→`live` in `integrations/registry.ts`. Migration `sql/migrations/019_salesforce_integration.sql` adds `deals.sf_opportunity_id`, `projects.sf_account_id` + `sf_primary_contact_id`, `founders.sf_contact_id` (+ inlined into `sql/schema.sql`) — **pending apply to remote D1**. **OAuth web-server flow** (PKCE S256): `?sandbox=1` on `/oauth/salesforce/start` chooses `test.salesforce.com` vs `login.salesforce.com`; the route layer (`routes/integrations.ts:840`) stashes `is_sandbox` into `oauth_state_tokens.extra_json` so the callback recovers it independently of the query string and passes it to `connect()` via `input.config` (callback now spreads `consumed.extra` into config — generic mechanism any future provider can use). Connect persists `instance_url` from the token response into `config.instance_url`; ALL subsequent API calls hit that per-org host (never a hardcoded one). **Refresh-on-401**: `sfFetch` retries once after `refreshAndPersist` when SF returns 401 INVALID_SESSION_ID — same KV-lease single-flight pattern as HubSpot (`salesforce:refresh:<uid>`). **Bidirectional sync** (`sync()`): outbound pushes deals modified since `last_synced_at` (status ∈ studio enum); inbound runs SOQL `WHERE LastModifiedDate > <iso>` against Opportunity and only updates local rows already keyed by `sf_opportunity_id` (no auto-create — would require synthesizing a project). Cron sweep at `index.ts` minute % 30 via `syncAllSalesforceIntegrations`. **Auto-push hooks**: deals.ts stage-change + projects.ts create now `schedulePush` to BOTH hubspot AND salesforce (no-op when not connected). **Field mapping**: `DEFAULT_FIELD_MAPS` (opportunity/account/contact studio→SF maps) + `DEFAULT_STAGE_MAP` (Prospecting/Qualification/Proposal/Closed Won/Closed Lost). Custom `Axal_*__c` fields are skipped on retry when SF returns INVALID_FIELD (most orgs won't have them). Admin overrides via `validateConfig` allowlist `stage_map`/`field_map`/`is_sandbox`/`instance_url`; SF field API names regex-validated `^[A-Za-z][A-Za-z0-9_]*$`. **Frontend**: `IntegrationsPage.jsx` ConnectModal renders Production/Sandbox radio for `provider.key==='salesforce'` before OAuth start (passes `?sandbox=1`); connected card surfaces env+username+orgId; new `SalesforceConfigModal` powers stage+field mapping (uses provider actions `list_stages` reading Opportunity describe picklist + `list_field_map` returning current+defaults). `api.js` `integrationsOauthStart` now accepts query params. **Required env**: `SF_CLIENT_ID`, `SF_CLIENT_SECRET` — provider throws `salesforce_oauth_unconfigured` if missing. Connected App must whitelist redirect URI `${APP_URL}/api/integrations/oauth/salesforce/callback`. Disconnect calls SF `/services/oauth2/revoke` with refresh token (revokes both). Webhook surface explicitly NOT shipped — CometD/Platform Events out of scope; 30-min poll is the safety net. Worker `tsc --noEmit` and frontend `vite build` both pass; `npm audit` 0 vulnerabilities.