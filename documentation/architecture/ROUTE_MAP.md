# ROUTE_MAP.md — canvas → route → backend, with implementation status

Produced by the P−1 audit (§16 step 2). Every "Live route today" cell was read
from `frontend/src/App.jsx` with its guard; every backend cell names a file that
exists under `cloudflare-worker/src/routes/`. CURRENT vs UPGRADE calls come from
reading the live page component side by side with the canvas, not from name
matching. Comparisons are source-level; the app was not run.

**Corpus:** 107 canonical `*.dc.html` canvases at `design/canvases/` (top level
only). `uploads/` holds 3 stale duplicates and `scraps/` holds images — both
excluded. Also at the root, not counted in the 107: 2 standalone pitch-deck
HTMLs, 4 shared JS modules (`support.js`, `doc-page.js`, `deck-stage.js`,
`fund-model.js`) and `assets/`.

## Status taxonomy

| Status | Meaning |
| --- | --- |
| `CURRENT` | A live route implements this canvas and the shipped UI matches. Do not rebuild. |
| `UPGRADE` | A live route exists but the canvas evolved past it. Diff and re-integrate — never skip as "already done". |
| `RESKIN` | Backend/services are live; the shipped UI predates the canvas. |
| `NEW` | No live equivalent. |
| `OUT OF SCOPE` | Spin-Out Lab canvas. The Lab is live and plugged; not a work item. |
| `DEFERRED` | Persona hub canvas. Held until `/studio`'s future iteration. |

---

## Founder / admin / advisor / partner — part 1 (29 canvases)

| Canvas | Persona | Proposed route | Live route today | Backend it wires to | STATUS | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| 83b Election Tracker | founder | `/spinout-lab/83b` | `/spinout-lab/83b` (`/incorporate/83b` 301s to it, App.jsx:1849) | `legal_83b.ts`, `captable.ts`, `legal.ts`, `projects.ts` | OUT OF SCOPE | Lab tool. `SpinoutLab83bPage.jsx:15` names the canvas as its design handoff and documents which mock content was deliberately not reproduced. |
| AIRail | chrome | — (right-rail component, no route) | — | `monitoring.ts` (`GET /monitoring/ai-usage`) + `services/aiRouter.ts`; no per-user rail endpoint | NEW | Per-page "Worker AI" rail: mode toggle, per-page model picker with $/M pricing, month-to-date spend vs a $40 cap, per-page spend, receipt line, Llama-Guard "Screened" badge. `aiRouter.ts` already enforces day/month/org caps but exposes no user-facing spend or model-choice API. |
| Account | shared | `/settings`, `/settings/:section` | same | `settings.ts`, `users.ts`, `kyc.ts`, `billing.ts`, `integrations.ts`, `company.ts`, `trust.ts`, `refer_earn.ts` | UPGRADE | Live has 8 sections. Canvas adds 4 panes: Identity & tax (gov-ID, accreditation evidence, LinkedIn import), Your companies (multi-entity switcher repointing the sidebar), Roles & access (multi-role licences, delegates — no delegate concept exists), Documents & agreements. Notification *presets* replace the 17-row matrix. Sessions, API keys, trusted contacts already ship. UPDATE 2026-08-29 (Wave 2): **Your companies** and **Documents & agreements** shipped into the Account section — SettingsPage made zero company/e-sign calls before, while `/company/memberships` and `/legal/esign` were both live. Your companies is read-only by design (switching stays in `ui/CompanySwitcher.jsx`). Still open: Identity & tax (gov-ID/accreditation evidence beyond the existing Verification tab), and Roles & access — the canvas's delegates concept has no schema, so it is not buildable. Pinned by frontend/test/company_settings_members.test.mjs. |
| Admin · Subsidiary | admin | `/admin/subsidiary` (new) | — | — (none) for tenancy; adjacent `admin.ts`, `admin_contracts.ts`, `admin_cohort.ts`, `admin_lp_applications.ts`, `admin_events.ts` | NEW | Territory-licensee console: territory badge filtering every query, seats licensed-vs-used, five approval queues collapsed to one SLA board, HQ-owned read-only template library, anonymised benchmarks. No multi-tenant model exists. **UPDATE 2026-08-29 (Wave 4):** the licence LEDGER now exists (migration 187, `/admin/licences`) — territories, seats, terms and renewals per subsidiary. This row stays gated: it needs the SCOPING half, where an account names the licence it belongs to, and no row carries one. |
| Admin · Super | super-admin | `/admin` | `/admin` + `/admin/*` | `admin.ts`, `admin_billing.ts`, `admin_stripe.ts`, `admin_promos.ts`, `admin_contracts.ts`, `admin_articles.ts`, `admin_publications.ts`, `admin_news.ts`, `admin_integration_keys.ts`, `monitoring.ts`, `activity.ts` | UPGRADE | Live covers accounts + View-As impersonation with audit, revenue/Stripe/promos, content, integration keys, infra/cron/DLQ. Net-new: the licensing tier (licence ledger keyed on territory, 5-step issue flow whose step 2 hard-blocks on territory overlap, rev-share splits, renewals), tenant switcher + read-only overlay, subsidiary-health cards, HQ escalation queue, token P&L per subsidiary, one content pipeline replacing three. **UPDATE 2026-08-29 (Wave 4): the LEDGER half of the licensing tier shipped** — migration 187 (`territory_licences`, `licence_territories`, `licence_seats`, `licence_events`), `routes/admin_licences.ts` mounted at `/api/admin/licences` before the `/api/admin` catch-all and added to `COOL_OFF_PREFIXES`, and `/admin/licences` → `AdminLicences` with the five-step flow (Entity → Territory → Seats → Terms → Activate). Territory exclusivity is a UNIQUE index on `licence_territories.country_code` **alone** — not `(licence_id, country_code)`, which would only stop a licence duplicating its own row and would let two licences hold France. The picker refuses an overlap rather than flagging it, per the canvas: "a conflict found after signature is an amendment to two contracts, found here it is one click." **A suspended licence still holds its territory**; rows are deleted on terminate and untouched on suspend, so no code path can forget the rule. The fee is integer cents and both rates are integer basis points (3500 = 35%) — `_bps` was added to the money guard's rate-suffix list so a percentage is not asked for cents. **NOT shipped, and this is the important part: the SCOPING half.** No existing query learns a territory from this and no row gains a `licence_id`. Retrofitting that across 151 route files is a programme, and the repo's rule is that tenancy goes through ONE middleware — a half-applied scope reads as enforced and is not, which is worse than none. The visible cost is that seats used, accounts per subsidiary, revenue per subsidiary and the token P&L are reported as **unavailable with the reason**, never as zero: "0 of 325 seats used" is a false statement about a real business. The four licensees in the canvas are placeholders and are not seeded. |
| AdminRail | chrome | — (right-rail component) | — | `monitoring.ts` + `services/aiRouter.ts` | NEW | Admin variant of the AI rail: adds an admin-assist op line, plan-based cap, an Axal VC margin figure, tenant-scope chip. |
| AdvRail | chrome | — (right-rail component) | — | `monitoring.ts` + `services/aiRouter.ts` | NEW | Advisor variant ("Calendar, ledger and deliverables work alone. No tokens."). |
| Advisor Canvas | advisor | `/studio`, `/advisor/advisory/*`, `/office-hours`, `/network`, `/signals` | `/studio`, `/advisor/advisory/*`, `/office-hours`, `/network`, `/signals` | `advisors.ts`, `esign.ts`, `introductions.ts`, `partnernet.ts`, `signals.ts` | UPGRADE | Six-row advisor shell is live: Home, Spin-Out Lab, Practice, Expertise, Network, Research. Practice keeps canonical `/advisor/advisory/*` tabs; Expertise composes live `/office-hours` profile, availability, pricing, bookings, and reviews; Network and Research retain scoped live data with Advisor-specific emerald/cyan framing. |
| Advisor Detail · Practice | advisor | `/advisor/advisory/*` (canvas proposes `/practice/*` — do not mint that) | `/advisor/advisory/opportunities` · `/clients` · `/engagements` · `/delivery` · `/contracts` | `advisory.ts`, `advisors.ts`, `consultations.ts`, `esign.ts`, `payments.ts`, `orders.ts` | UPGRADE | Canvas drops Clients/Contracts as top-level, adds Sessions (availability rules, session types, booking links, per-slot recording state) and Earnings. Within shared tabs: decision history + terms templates, renewal history, scope **exclusions** as first-class text, deliverable version-trail + opened/unopened. UPDATE 2026-08-29 (Wave 1b): all five tabs are live-wired against `advisors.ts` (profile, slots, bookings, transitions, reviews) + `esign.ts` — fixture deleted, pinned by frontend/test/advisor_advisory_live.test.mjs. D12 had scoped this out by checking `advisory.ts` only; see DECISIONS.md **D31**. Remaining canvas deltas: session-type pricing and earnings (that is #124 / `/office-hours`, still frozen), private client notes, and the document deliverable version-trail (no store — the Delivery tab says so on-screen). |
| Advisor Studio | advisor | — | `/spinout-lab/advisors` (admin side) | `advisors.ts`, `spinout_lab.ts`, `assessment.ts`, `esign.ts` | OUT OF SCOPE | **Re-tagged from DEFERRED.** Content is the Spin-Out Lab advisor-program application + admin access-decisioning gate (Cohort 4 · Week 2), not a `/studio` persona hub. |
| Advisors | founder | `/spinout-lab/advisors` | `/spinout-lab/advisors` | `advisors.ts`, `scoring.ts`, `values.ts`, `projects.ts`, `spinout_lab.ts` | OUT OF SCOPE | Lab tool. `SpinoutLabAdvisorsPage.jsx:3` names its handoff and documents fabricated canvas content not reproduced. Distinct from the platform-wide `/advisors` directory. |
| Advisory Practice | advisor | `/office-hours` | `/office-hours` | `advisors.ts`, `consultations.ts`, `calendar.ts`, `payments.ts`, `orders.ts` | UPGRADE | Live is profile + slots + bookings + review (398 lines), now composed under Advisor Expertise as the canonical storefront editor. Canvas additions still not modeled: session-type pricing catalog, earnings ledger, private client notes, and consented featured proof. Unsupported areas remain explicit rather than using illustrative values. |
| Apply and Status | founder | `/spinout-lab/apply` | `/spinout-lab/apply` | `spinout_lab.ts` | OUT OF SCOPE | Lab apply flow + status screen. `SpinoutLabApplyPage.jsx:8` cites the canvas APPLY VIEW. |
| Auth and Onboarding | public / shared | `/login`, `/register`, `/onboarding/persona`, `/onboarding/founder\|investor\|partner` | same | `auth.ts`, `auth_google.ts`, `auth_passkey.ts`, `auth_sms.ts`, `auth_recover.ts`, `onboarding.ts`, `onboarding_checklist.ts`, `personas.ts` | UPGRADE | Magic link, Google SSO and passkeys already ship. Canvas re-ranks: magic link *primary*, SSO secondary, no password at all; adds an "Apply to Axal VC" route for non-members; reframes role as **licence** choice ("sets your workspace, not your permissions… each is bought and onboarded separately"); specifies **four** wizards including advisor — live has three. |
| Axal VC Website | public | `/`, `/about`, `/for-founders`, `/for-investors`, `/spinout-lab/brief`, `/contact`, `/articles`, `/directory` | same set | `public.ts`, `insights.ts`, `articles.ts`, `events_public.ts`, `network_public.ts`, `team_public.ts`, `contact.ts`, `catalog.ts` | UPGRADE | Full 8-section marketing site. Live `LandingPage.jsx` has only how-it-works / platform / latest / events; `PublicNav.jsx` links just `/`, `/login`, `/register`. Net-new: the Firm narrative (mission, weighted investment philosophy, Fund I terms — `/about` currently renders `TeamPage`), Cohort-5 funnel with a close date, underwriting-edge comparison table, LP-experience section, contact form with a stated 5-business-day reply. **Not built — blocked on facts only the firm holds.** The Firm narrative asks for a weighted investment philosophy and Fund I terms; those are fiduciary statements about a real fund and cannot be drafted from the codebase. The Cohort funnel needs the live cohort close date (sourced already — see #42) but the surrounding narrative does not exist anywhere in the repo. The 5-business-day reply is a commitment, not a field: `contact.ts` stores submissions and nothing measures or promises a response time. |
| BD Console | partner | `/partner/operations/*` + `/needs` | `/partner/operations/overview` · `/capabilities` · `/portfolio` · `/engagements` · `/performance`; `/needs` | `needs.ts`, `partner_portal.ts`, `partners.ts`, `services.ts`, `matches.ts`, `payments.ts`, `orders.ts` | UPGRADE | Six zones incl. a proposal builder (engagement type, scope pulled from the RFP, milestone/retainer pricing, attached case studies, preview-as-founder, accept & sign) and an invoice ledger — neither exists anywhere. `needs.ts` already has `/:id/rfp` and `/:id/quotes`. UPDATE 2026-08-29 (Wave 1a): all five operations tabs are now live-wired — fixture deleted, proposal builder (RFP-prefilled) + invoice ledger + real analytics shipped; pinned by frontend/test/partner_operations_live.test.mjs. Remaining canvas deltas: richer firm-profile fields beyond the thin partners row, case-study authoring (no schema), Stripe-backed invoices (transition writes stub ids). |
| Brand & Landing Page | founder | `/spinout-lab/brand` | `/spinout-lab/brand` | `brand.ts` | OUT OF SCOPE | Lab tool. `SpinoutLabBrandPage.jsx:3` cites this file and maps every zone to a real endpoint. |
| Brand & Landing Page copy | — | — | — | — | OUT OF SCOPE | **Stale duplicate.** Byte-identical to the above except it is missing three `.bl-root.bl-embedded` CSS rules — the older pre-embed copy. Should not count as a distinct surface; recommend deleting. |
| Cap Table Pro | founder | `/raise/capital/cap-table` (also `/build/captable`) | `/raise/capital/cap-table`, `/build/captable`, `/share/captable/:token` | `captable.ts` (incl. `GET/POST /captable/409a/:projectId`), `services/captableSchema.ts`, `services/captableShare.ts`, `legalcap.ts` | UPGRADE | Live (1053 lines) has ownership, dilution, waterfall, one option-pool reference. Canvas adds per-SAFE conversion detail with cap-vs-discount and MFN inheritance, the pre-money/post-money "who pays for the pool top-up" double reading, a grant-level pool burn-down ledger with vesting, and a 409A safe-harbour panel — **the 409A backend already exists with zero UI**, so that one zone is a pure RESKIN inside an otherwise UPGRADE canvas. UPDATE 2026-08-29 (Wave 2): the 409A panel was already shipped by an earlier pass (`captable/SafeHarbourPanel.jsx`, mounted in CapTablePage) — that census line was stale. New this pass: the **pool burn-down + vesting ledger** (`GET /captable/equity-plan`, `captable/EquityPlanPanel.jsx`). `cap_table_option_pools` and `cap_table_vesting` have existed since migration 057 and are written on every Carta sync; the only worker file that named either was the importer, so a founder who connected Carta had this in D1 and no surface showed it. **Still not built, and not buildable: per-SAFE cap-vs-discount and MFN inheritance.** Those terms are not stored — `valuation_cap` exists only on `deals` (investor pipeline), `discount_rate` appears in no table, and MFN exists only as prose inside a legal template. Deriving a conversion from terms the database does not hold would be inventing fiduciary numbers. Pinned by frontend/test/captable_equity_plan.test.mjs, which fails if those columns ever appear. |
| Cap Table | founder | `/spinout-lab/captable` | `/spinout-lab/captable` | `captable.ts`, `legal_83b.ts`, `projects.ts`, `spinout_lab.ts` | OUT OF SCOPE | Lab tool ("Unlocked · Wk 4", Carta sync). |
| Capital | founder | `/spinout-lab/capital` | `/spinout-lab/capital` | `contacts.ts`, `captable.ts`, `scoring.ts`, `progress.ts`, `projects.ts` | OUT OF SCOPE | Lab tool. **Not** the investor-side `/capital` (`CapitalPage.jsx`) — different surface, same word. |
| Co-founder Agreement | founder | `/spinout-lab/cofounder-agreement` | `/spinout-lab/cofounder-agreement` | `legal.ts`, `cofounder.ts`, `captable.ts`, `projects.ts` | OUT OF SCOPE | Lab tool; clause builder prefilled from Cap Table / Incorporate / Co-founder Match. |
| Co-founder Match | founder | `/spinout-lab/cofounder-match` | `/spinout-lab/cofounder-match` | `cofounder.ts`, `values.ts`, `radar.ts`, `scoring.ts`, `projects.ts` | OUT OF SCOPE | Lab tool ("Unlocked · Wk 3"). |
| Co-marketing | partner | `/comarketing` | `/comarketing` | `comarketing.ts` | UPGRADE | Live (430 lines) does pitch submit, admin approve/reject/publish, published catalog, an AttributionBadge. Canvas adds a dismissible first-run explainer, an "Angle — what makes this worth an audience's time" field plus "what you bring", a published-detail modal with headline stats and an inbound-leads-by-week chart, a channels breakdown, and a recent-leads handover list. Tabs change from *My pitches / Review queue* to *My pitches / All pitches*. **Wave 2 shipped** the detail drawer: `api.listMyCoMarketingAttributions` had been in `api.js` with no caller since T15, so every tracked visit/signup/lead was in D1 behind a four-number badge. The drawer now draws leads-by-week, a referrer breakdown and the recent-lead list from those rows. **Not shipped, and why:** per-channel *reach* is recorded in no column, so the proposed channels render as the prose they are with no number attached; lead *names* are not stored, so initials derive from `lead_email`; **"Angle" and "what you bring" now exist** (migration 183, two nullable TEXT columns, no backfill) — collected on the pitch form, shown on the card and drawer, and leading the admin review row, since the canvas copy is explicit that approval turns on the angle. Tab rename not taken: "All pitches" is the admin queue, which is a different permission, not a different label. |
| Company Settings | founder | `/company-settings` | `/company-settings` | `company.ts` (`/company/create`, `/companies`, `/company/memberships`, `/company/:uid/members` POST+DELETE) | UPGRADE | Live is 289 lines rendering one profile card. Canvas is 4 zones plus an empty-state on-ramp ("Create your company" / "Join an existing one") and a company switcher: Public profile, Growth details (members-only, feeds investor matching), Members & access (invite by email, role change, remove, primary-admin marker, invited-can't-read-Growth-details), Workspace & lifecycle. **Member CRUD already exists with no UI** — that zone is a RESKIN. The canvas's own Danger zone is labelled inert. UPDATE 2026-08-29 (Wave 2): Members & access shipped — the member list was already in the page's state from `GET /company/:uid` and simply never drawn; add/remove wired, role change + primary-admin transfer added (`PATCH /company/:uid/members/:userId`, guards mirroring POST). Empty state is now a working `POST /company/create` on-ramp. Pinned by frontend/test/company_settings_members.test.mjs. Not done: joining is stated as unavailable (no join-request or invitation-accept endpoint exists), and the company switcher already lives in `ui/CompanySwitcher.jsx` — not duplicated here. |
| Compliance | founder | `/spinout-lab/compliance` | `/spinout-lab/compliance` | `legal_83b.ts`, `legal.ts`, `projects.ts`, `spinout_lab.ts` | OUT OF SCOPE | Lab tool. `SpinoutLabCompliancePage.jsx:25` names the canvas and states it does **not** replace `/compliance`, the platform-wide compliance *calendar* backed by `compliance.ts`. |
| Contracts · Subsidiary | admin | `/admin` (Legal / Agreements tabs) | `/admin` (`tab=legal`, `tab=agreements`); `/esign/:token` | `admin_contracts.ts`, `esign.ts`, `legal.ts`, `admin_forms.ts`, `services/legalTemplateStore.ts` | UPGRADE | E-sign backend is deep and a `NewEnvelopeWizard` ships. Canvas adds offers and contracts as one pipeline (structured terms → negotiation → envelope), a 5-step send flow where every AI-filled merge field stays visibly tinted until a human confirms, per-recipient signature progress, an expiring-soon board (NDAs by `valid_until`, agreements by renewal) with renew/lapse/snooze, a contract dossier with tamper evidence and an immutable timeline, and territory scoping. |
| Contracts · Super | super-admin | `/admin` (Legal → Templates) | `/admin` (`tab=legal`, `AdminTemplates.jsx`) | `admin_contracts.ts` (`/templates/store*`, `TEMPLATE_LAYERS`), `esign.ts`, `legal.ts` | UPGRADE | Live does template CRUD, version history, preview PDF, the four governance layers. Canvas adds a tier above: a doc-type registry (what a contract may *be*) with an add-type editor covering layer, party roles, required fields, availability/blast radius, plus overlap detection and merge; adoption metrics rather than just versions; a cross-tenant oversight ledger; a per-tenant compliance-layer monitor. |
| Customer Acquisition Landing Page | founder | `/spinout-lab/brand` (template `customer-acquisition`) | `/spinout-lab/brand` → that template | `brand.ts` | CURRENT | Ships as one of 16 templates in `lib/brand/templates.js`, rendered by `CustomerAcquisitionPreview.jsx`, identical section set. One delta: the canvas pairs a hideable "Backed by" logo wall with the testimonial; live ships the testimonial only. Zone-level strictness would read UPGRADE. |

## Founder / investor / advisor / partner — part 2 (26 canvases)

| Canvas | Persona | Proposed route | Live route today | Backend it wires to | STATUS | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Customer Audience Landing Page | public (founder-authored) | `/spinout-lab/brand` (author surface) | `/spinout-lab/brand`, `/build/brand`; published at `/p/:site/:page` and `/landing/:slug` (worker-served, not SPA) | `brand.ts` + `services/landingTemplates.ts` | CURRENT | Ships verbatim as template `customer-audience`. `TEMPLATE_DESIGN_SOURCES` names this exact file; `renderCustomerAudience()` renders the published page. Audience switcher is CSS `:checked`-driven for CSP. |
| Customer Discovery | founder | `/spinout-lab/discovery` | `/spinout-lab/discovery`; full tool at `/build/discovery` | `progress.ts`, `brand.ts` | OUT OF SCOPE | Lab tool; the live page names this design handoff explicitly. |
| Customer Templates | founder | `/spinout-lab/brand` | `/spinout-lab/brand` (template library zone); builder at `/build/brand` | `brand.ts` | OUT OF SCOPE | Lab tool — the canvas says templates "prefill from the startup profile you've already built elsewhere in the Lab". |
| Data Room | founder + investor-LP | `/raise/data-room` (new) | — | partial only: `projects.ts` (`data_room_url`, `data_room_nda_required`), `deals.ts` (`GET /:id/data-room` → zip), `files.ts`, `esign.ts`, `pairwise_ndas` table | NEW | Nothing implements a data room as an object — today it is a free-text external URL plus a zip of document text. Needs a folder/file model with per-folder and per-file visibility, per-investor grants, an NDA gate unlocking protected files, watermarked downloads, view/download log. The investor half is entirely new. Can sit on the existing `pairwise_ndas` + `esign.ts` rail rather than inventing one. **Wave 4 shipped** it on exactly that rail: migration 184 (folders, files, per-investor grants, access log), `routes/data_room.ts`, and `/raise/data-room` role-branched inside one route so no second root appears. A file is `open` or `nda`; `nda` needs a grant AND a live `pairwise_ndas` row, re-checked at download rather than inherited from the listing. **Not built, and why:** no watermarking — the worker has no PDF pipeline, so a download is per-investor, single-use, two-minute TTL and logged, and the UI says that instead of implying a watermark; no preview separate from download, since streaming bytes to a browser IS giving someone the file. An NDA-gated file is **withheld, not shown locked** — a filename is itself information — so the investor gets a count of what is behind the gate. |
| Deal Flow | investor-LP (GP) | `/pipeline` + `/deals/:dealId` | `/pipeline`, `/pipeline/screening\|commit\|transactions`, `/deals`, `/deals/:dealId` | `deals.ts`, `pipeline.ts`, `scoring.ts`, `ic.ts` | UPGRADE | New: stage-SLA config + hygiene strip; a **pass modal with a required, queryable reason** plus a "Why we passed" analytics zone (no `pass_reason` anywhere in the worker); source-quality table ranked by term sheets/yield; stage funnel with per-stage conversion and avg-days-over-SLA; deal-record right rail. Live `/deals/:dealId` has only Overview/Documents/Commitments/Activity. |
| Detail Layer Canvas II | advisor + partner | — (IA spec, not one screen) | — | — (none) | NEW | Companion spec for the Advisor (23 pages) and Partner/Operator (22 pages) licences. Adds two obligations the founder/investor half lacks: **consent state** on session-derived content, and **granted scope** on operator seats (enumerated, revocable, owned by the founder who issued it). Routes it assumes do not exist. Scope as a programme, not one route. |
| Detail Layer Canvas | founder + investor-LP | — (IA spec, not one screen) | — | — (none) | NEW | Defines an "expansion contract": every workspace zone compresses on the workspace and expands to exactly one of six archetype detail pages (Work Board, Ledger, Collection, Feed, Match Engine, Analytics) — 45 pages, 26 founder + 19 investor. Also specifies the AI rail's detail-page behaviour (inherit the workspace model; batch ops with a pre-run estimate, each item landing as its own proposal). Scope as a programme. |
| DetailRail | chrome | — (docked rail component) | — | `services/aiRouter.ts` | NEW | Right-docked "Worker AI" rail for a **detail** page: mode + model shown as INHERITED from the parent workspace, batch operation with a pre-run cost estimate, spend meter, "Screened" chip. No live equivalent. |
| Due Diligence | investor-LP (+ founder side) | `/due-diligence`, `/due-diligence/:uid`, `/due-diligence/requests` | same, plus `/admin/due-diligence*` | `dd.ts` (1157 lines: catalog, cases, scan, section assign/verdict, item patch with owner+due+severity+note, requests, report + share + signed download, audit, reviewer invites, per-section NDA) | UPGRADE | Most of the canvas ships. New and narrow: a per-fund **section-weighting editor that versions the template**; the **blocking-section rule** (a blocking section cannot carry an open flag at IC — no "blocking" concept exists in `dd.ts`); an explicit **sign-off column** per section; and "Cross-checks / independent verification" framing over the shipped "External sources" table. |
| Emails | shared | — (email template layer) | — | `templates/email/registry.ts` (49 templates), `templates/email/layout.ts`, `services/email/send.ts`, `routes/email.ts`, `notifications.ts` | UPGRADE | The M0 half is live: single `send()` entry, shared chrome with registered address + preferences link, RFC 8058 one-click List-Unsubscribe honouring `users.marketing_unsubscribed_at`, `email_send_log` + queue + DLQ. New: 4 persona **broadcast invitations** (Set A) and 4 **GP personal letters** (Set B — lavender greeting band, `[First Name]` merge field, no unsubscribe, typed sign-off). Registry has none of these 8. |
| EmberRail | chrome | — (docked rail component) | — | `services/aiRouter.ts` | NEW | Near-identical sibling of DetailRail — same four blocks, different mode copy. Treat DetailRail + EmberRail as **one** rail spec with two wordings. |
| Events | shared / public | `/events` + `/events/:slug` | `/events`, `/events/:slug`, `/my/events`, `/events/new`, `/events/:id/edit`, `/events/:id/manage`, `/invite/:token` | `events.ts`, `events_public.ts` | UPGRADE | E1 is close to live (filters, programs, ICS). Canvas adds month-grouped sections with counts, replay chips, programme badge. E2 (Demo Day) is the real gap: countdown + livestream, a **running order** off the live agenda with per-presenter "Request intro" (no `requestIntro` exists), attending-roster preview, logistics, and a post-event variant of the same URL (replay + "Your recap" with intros made). `/lp/spinout-demo-day` is a marketing page, not this. **Wave 3 shipped** E1's month-grouped sections with per-month counts and a date tile, plus two defects the canvas work uncovered: `?past=1` only *dropped* the upcoming predicate, so the Past tab returned upcoming and past together oldest-first (it is now its own range, most-recent-first, and an event stays "upcoming" until it ENDS rather than until it starts); and the page requested the API's default `LIMIT 20` with no offset handling, so a 21st public event did not exist and nothing said so (now pages at 50 with Load more). **Not shipped, and why:** the replay chip and the past-event "View recap" CTA — `events` has no `recording_url`/`replay_url` column, so the CTA now reads "View details"; per-presenter Request intro (no endpoint); the attending roster (no public endpoint, and it would expose registrants); E2's post-event recap (nothing stored). Type-filter counts skipped: the type filter is server-side, so counts would be over a partial page. |
| Explore the Spin-Out Lab | investor-LP | `/spinout-lab` | `/spinout-lab` (investor branch) | `spinout_lab.ts`; content in `lib/spinoutInvestorContent.js` | OUT OF SCOPE | `SpinoutLabInvestorPage.jsx:2` states it is a native port of this canvas with four documented departures. |
| ForgeRail | chrome | — (docked rail component) | — | `services/aiRouter.ts` | NEW | Third rail variant, materially different: branded "Eadwyn" (D3, resolved — the canvas's own working name was "Forge"), model **RECOMMENDED** not inherited, discloses an explicit "Axal VC margin" line, scope chip, and a hard boundary — "Eadwyn never sends, signs or voids; every outbound action is a human click." The three rail namings were mutually exclusive product voices; D3 picked Eadwyn as the single one to build. |
| Founder Journey Map | admin (internal) | — (audit artifact, not a surface) | — | — (none) | NEW | Not a screen to ship: a journey map reconstructing 10 steps across 6 phases with per-step health tones, arguing the journey is strong at the workspace and broken on both sides of it. No seeded data. Treat as an audit input. |
| Founder Studio | founder | `/studio` | `/studio` (Dashboard) | `dashboard.ts`, `assistant.ts`, `tickets.ts`, `progress.ts`, `advisor.ts` | DEFERRED | Persona hub — no diff performed. |
| Founder Wellbeing | founder | `/wellbeing` | `/wellbeing` (guard `['admin','founder']`), `/wellbeing/expert-dashboard`, `/wellbeing/expert/:uid` | `wellbeing.ts`, `founder_risk.ts` | UPGRADE | Live (966 lines) ships the 6-dimension encrypted check-in, tags, history, matched experts with `match_breakdown`, booking, resource directory. New: a **30-day trend** panel over the existing `/aggregate`; a **"What the pattern shows"** zone reading observations against company context; a **pinned crisis card + full-screen overlay** carrying the promise "Axal VC does not see that you opened this"; a reminder-cadence picker; **"Export my data"**. Grep confirms none of these strings exist live. |
| Founder Workspaces Canvas | founder | — (IA spec, 6 workspaces) | scattered `/build/*`, `/raise/*`, `/founder/growth/*`, `/network`; no Validate root, no founder Research | `progress.ts`, `decks.ts`, `capital.ts`, `captable.ts`, `network.ts`, `market_intel.ts`, `aiRouter.ts` | NEW | 7 artboards: the shared Worker-AI rail spec (mode/model/meter/safety, 44–52px collapsed spine, dark variant, and the proposal-anatomy rule — cost shown before the run and receipted after, a proposal never writes to the record, amber strike-through + second confirm for overwrites, provenance mark on accepted output) plus six consolidated workspaces: Validate, Build, Raise, Grow, Network, Research. Parent spec for the three Rails and for Detail Layer Canvas. |
| Fund Administration | admin (GP) | `/funds`, `/funds/capital-calls`, `/funds/accounting` | those + `/funds/performance`, `/funds/lp-workspace`, `/lp-reports`, `/portfolio/waterfall` | `funds.ts`, `legalcap.ts`, `lp_reports.ts`, `fund_simulator.ts` | UPGRADE | Live: fund create (vintage/carry/mgmt fee only), capital-call list, distributions, LP reporting with TVPI/DPI, exit waterfall. New: a deliberately slow **fund-setup wizard** (fee step-down, preferred return, waterfall type, term + extensions, GP commitment, hard cap at 125% of target, fee-load projection); a **capital-call builder** with editable per-LP pro-rata and notice preview; a **collection tracker** with aging and per-LP reminders; a printable **LP capital-account statement**; **GP economics** (fee ledger accrued vs collected, carry unrealised until the pref clears, GP commitment funded). `/funds/accounting` is static demo data. |
| Fund Brief One-Pager | investor-LP | download action on `/spinout-lab` + `/spinout-lab/investor-workspace` | same | — (none; client-side vector PDF) | CURRENT | Fully implemented as `lib/fundBriefPdf.js` (jsPDF vector, geometry shared with the quarterly report via `lib/pdfBoard.js`) reading `lib/fundBriefViewModel.js`. That view model is a port of this canvas with the design's private `F = {…}` literals deliberately removed so figures cannot diverge from the LP workspace; `fund_brief_model.test.mjs` fails if a literal creeps back. Only the masthead photo and logo bitmap are intentionally not reproduced. |
| Funds · Fabric | super-admin | `/admin/fabric` (new); fund console reuses `/funds` | — | `legal.ts` (entities CRUD: `entity_type IN (holding_company, project, subsidiary, vc_fund)`, `parent_id`, `jurisdiction`), `funds.ts`, `compliance.ts` | NEW | 10 artboards defining a multi-tenant HQ/subsidiary fund fabric: entity graph with tinted tenancy hulls (explicitly "not a permission check over a shared graph: a different query"), entity dossier with filings calendar, fund console in two modes (HQ working-GP vs read-only cross-tenant Oversight with FX normalisation and the brand-licence share), subsidiary variant showing the HQ share as its own waterfall tier, LP record with payment-reliability history, launch-a-fund, capital-call flow, jurisdiction packs, honesty states. Today `/legal` renders a flat entity list. |
| GP Application Review | admin | `/admin/lp-applications` | `/admin/lp-applications` | `admin_lp_applications.ts` | UPGRADE | The live page names this handoff and ports the split queue/detail layout. Not built: **bulk approve/decline**, **side-by-side comparison** of 2 applications, **Export CSV**, **median-decision-time**, and the "Qualification signals (derived, not a score)" panel. Keep one deliberate correction: the canvas asserts "Approvals grant reporting access immediately" — the page must not, because approval only unblocks LPA issuance and access follows the countersigned LPA. |
| Get Paid & Invoicing | partner (+ founder pay side) | `/services` + `/needs` / `/build/marketplace` | those; `/payouts` redirects to `/referrals` | `needs.ts` (needs → RFP → quotes → accept → engagement → `POST /:id/invoice`, Stripe Connect onboard/refresh), `payments.ts`, `orders.ts`, `services/invoiceEmails.ts` | RESKIN | The whole flow exists on the worker; the UI predates the canvas. Stripe Connect is a `<dl>` of "Charges enabled / Payouts enabled"; invoicing is one button opening a hosted `stripe_invoice_url`. Canvas designs a Get-Paid setup screen with three states, an **in-platform invoice document** (masthead, bill-to, engagement reference, line items carried from the accepted quote, VAT, totals), and a 4-state action rail. **CORRECTION + FIX 2026-08-29.** This row described the FastAPI in `backend/`, which is Replit-dev-only and never deployed. On the production worker `POST /engagements/:id/invoice` set `status = 'invoiced'` and `invoice_id = 'stub-<uid>'` and nothing else; D1's `engagements` has no `stripe_invoice_url` and no `stripe_invoice_id` column, and `engagementDto` is a bare row passthrough — so all three UI references were dead: two invoice links that could never render and a guard that never hid the button (a second click 409'd). The engagement was left permanently marked **invoiced with no invoice in existence**, which is worse than a missing feature: the record asserted something untrue about money. **Fixed** by migration 188 (`engagement_invoices`, `invoice_number_seq`), `services/engagementInvoices.ts` and an in-platform invoice document in `NeedsBoardPage.jsx` — which is what the canvas asked for and needs no Stripe, so it does not reverse task #138's removal of Stripe Connect. Amounts are integer minor units throughout; the legacy REAL `quotes.price` is converted at exactly one line and snapshotted, so a later quote edit cannot change an issued invoice. Numbers come from a dedicated counter, not from parsing the last number back, and a void invoice keeps its number. **Not built:** payment collection. There is no payment rail; `paid` records that the partner says they were paid out of band, and the document says so to both sides. Still to do from the canvas: the Get-Paid setup screen with its three states. **The row conflated two different Stripe surfaces; they are not the same and only one exists.** (a) WELLBEING EXPERTS have REAL Stripe Connect: `wellbeing.ts` `POST /experts/me/stripe/connect` creates an Express account, requests transfers + card_payments, returns an account-onboarding link and stores `stripe_account_id`, and the `experts` table carries real `stripe_charges_enabled` / `stripe_payouts_enabled` columns — so the Charges/Payouts `<dl>` on `/wellbeing/expert-dashboard` is backed by live data, not a placeholder. The canvas's three-state Get-Paid setup screen IS buildable there, and the three states map onto the real ones (no account / onboarding incomplete / enabled). (b) MARKETPLACE PROVIDERS have none: `needs.ts` `GET /providers/me/stripe` returns `connected: false, detail: 'stripe_connect_not_configured'` and the onboard route 503s with the same reason. Those are HONEST stubs — they name their own absence — which is why they are not on the defect list above; the invoicing path was the one that claimed something untrue. A Get-Paid screen for marketplace providers cannot be built until someone decides whether Connect returns for them at all (task #138 removed the payouts backend deliberately). |
| Graduation Certificate | founder | `/spinout-lab/certificate` | `/spinout-lab/certificate`; public verify at `/verify/:token` | `spinout_certificates.ts` | OUT OF SCOPE | Lab credential. Issue / revoke / reissue / public verification / profile badge all exist. |
| Help Center | shared | `/docs` | `/docs`, `/docs/admin/*` | — (none; static JS modules under `pages/docs/sections/`, client-side fuse.js search) | UPGRADE | Live ships 14 journey-organised sections with search, per-subsection how-to/tips/pitfalls, related links, on-this-page rail. New: persona-aware grouping and per-article surface tags, suggested-search chips, a **"Popular this week"** ranked list, a "Still stuck?" block with a live status line to `/status`; on the article page a "Where this lives" block, a worked example in mono, a warning callout, an "Applies to" persona list, an "Open the surface" deep-link, and **"Did this answer it? Yes / No"**. Popularity and feedback both need a backend that does not exist. **Wave 3 shipped** the "Still stuck?" block with its live status line — `GET /api/public/status` was already serving `/status`, and the docs footer was a static sentence. The roll-up rule is now shared (`lib/statusOverall.js`) so the two pages cannot disagree mid-incident; extracting it also fixed an inline `[].every()` that reported a confident "Operational" on an empty probe list, which is why StatusPage's `unknown` pill had never been reachable. **Blocked, not skipped:** "Popular this week" and "Did this answer it?" each need a store that does not exist (view counts, article feedback). "Where this lives" / "Open the surface" / per-article surface tags need a `surface` route on each of ~98 subsections — content authoring, and a wrong deep link is worse than none. "Applies to" would render "everyone" on 97 of 98: only `admin.js` carries a `roles` array today. |
| Incorporate | founder | `/spinout-lab/incorporate` | `/spinout-lab/incorporate`; separate platform surface at `/incorporate`, `/incorporate/success`, `/incorporate/83b`, `/incorporate/cofounder-agreement` | `legal.ts`, `legal_83b.ts`, `compliance.ts`, `spinout_lab.ts`, `payments.ts` | OUT OF SCOPE | Lab tool ("Unlocked · Wk 4"). **Not** the same surface as the platform's `/incorporate`. Do not collapse them. |

## Investor / advisor / partner / chrome — part 3 (24 canvases)

| Canvas | Persona | Proposed route | Live route today | Backend it wires to | STATUS | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| InvRail | chrome (investor) | — (right-rail component on investor surfaces, no route of its own) | — | `assistant.ts`, `monitoring.ts` (`/monitoring/ai-usage`) | NEW | Per-page Workers-AI model picker (recommended model + `@cf/…` id + $/M price), run receipt, page spend vs plan cap, and a per-page outbound-screening note, keyed by investor page (Home/Deals/Portfolio/Axal VC Fund/Fund/Network/Research). Nothing like it ships: the only AI-cost surface is the admin `AiUsageTab` inside `/monitoring`; there is no per-page rail, no model choice, no spend meter. |
| Investor LP Canvas | investor-LP | `/studio` (existing persona hub) | `/studio` → `frontend/src/pages/Dashboard.jsx` | `dashboard.ts`, `deals.ts`, `portfolio.ts`, `funds.ts`, `market_intel.ts` | DEFERRED | Persona hub canvas (brief §DEFERRED). No fidelity diff performed. It is the investor/LP hub: Home brief, Deals→Commit room, Portfolio, Axal VC Fund I, Fund ops, Network, Research, each zone compressed with an AI accept/edit/discard proposal band. |
| LP Investor Workspace | investor-LP | `/spinout-lab/investor-workspace` (existing) | `/spinout-lab/investor-workspace` → `SpinoutLabLpWorkspacePage`; also `/lp-portal`, `/funds/lp-workspace` | `spinout_lab.ts`, `funds.ts`, `lp_reports.ts`, `admin_lp_applications.ts`, `capital.ts` | DEFERRED | Persona hub canvas (brief §DEFERRED). No fidelity diff performed. LP-side Spin-Out Fund I surface: apply/allocation window, key terms, commitment process, capital calls issued, LP data room, IC signal, message the GP. |
| Legal & Capital Engine | shared (founder + GP/admin) | `/legal-capital` | `/legal-capital` → `LegalCapitalPage` (tabs: Legal Engine, Capital Engine, Spin-Out, Diligence, LP Portal) | `legalcap.ts`, `legal.ts`, `esign.ts`, `capital.ts`, `lp_reports.ts`, `dd.ts` | UPGRADE | New vs shipped: structured **key-terms forms per instrument** (SAFE cap/discount/pro-rata; priced-round pre-money/pool/exclusivity) pre-filled from the deal record with visible provenance; document **preview → Send for signature** with sequential signers (`esign.ts` is live but this page never calls it — its "Sign" is a status flip via `PATCH /legal/docs/:id/sign`); **pro-rata breakdown + call history + distributions**; LP-report **read receipts** ("not yet opened") and a "Viewing as LP · read-only" mode. |
| Liquidity & Secondaries | shared (founder + investor) | `/liquidity` | `/liquidity` → `LiquidityPage` (tabs: Marketplace, My Portfolio, Exit Pipeline) | `liquidity.ts`, `positions.ts`, `captable.ts` | UPGRADE | Canvas has 6 zones vs 3 shipped. New: a **company-sponsored tender** flow (tender terms, eligible allocation, shares-to-tender election, participation window) — `liquidity.ts` has no tender endpoint at all; a **transfer-restriction gate panel** (ROFR clock, board consent, lockup, restrictive legend) each with a "what happens next"; a **settlement step tracker**; and a **net-proceeds preview at draft time** (shipped `POST /liquidity/listings/:id/proceeds` only runs post-listing). |
| Messages | shared | `/messages` (new) | `/messages` → `MessagesPage` (Wave 4) | — (none platform-wide). Adjacent partial stores: `contacts.ts` (`/:uid/reply` log), `advisor.ts` (`advisor_conversations`), `customer_chat.ts`, `needs.ts` (quotes) | NEW | One unified inbox across intro / co-founder match / role search / engagement / advisory threads, each thread pinned to its linked object (Engagement, Introduction, Match, Search, Service, Session) with a context rail and a per-type filter. Nothing in the SPA is a general user-to-user inbox; D1 has no `messages`/`threads` table (only `advisor_messages`, `customer_chat_messages`, `assistant_messages`). Needs a store + `messages.ts` worker route. **Wave 4 shipped** the store and the inbox: migration 185 (`message_threads`, `message_thread_participants`, `messages`), `routes/messages.ts`, and `/messages` role-branched inside one route. Membership is the ONLY key — `memberThread()` resolves a thread by uid AND a participant row for the caller, there is no admin override, and every `:uid` handler 404s rather than 403s so a non-participant cannot confirm a thread exists. Unread is derived from `last_read_at` per participant rather than a counter column, so it cannot drift. A thread may pin to a linked object (`subject_type` ∈ introduction, match, engagement, service, session, job) but both subject columns are nullable — a plain conversation is a first-class thread, not a special case. **Not built, and why:** the canvas calls this a *unified* inbox, and there is nothing to unify. D1's only two message-shaped stores are `advisor_messages` (an AI transcript, rows keyed user/assistant/tool/system) and `customer_chat_threads` (the Slack-bridged support desk) — neither is a peer conversation, so folding either in would misrepresent what it is. The inbox therefore starts empty and its empty state names where those two actually live instead of implying they were merged. No per-type filter rail ships until there are threads of more than one type to filter. |
| Metrics | founder | `/build/metrics` | `/build/metrics` → `MetricsPage` | `metrics.ts`, `progress.ts`, `financials.ts`, `portfolio_updates.ts`, `email.ts` | UPGRADE | Shipped page is one tab: snapshot entry form + history table + Stripe import + runway summary. Canvas adds three more: **Investor update** composer (narrative, recipients, send-test-to-myself, send log), **Board pack** builder (pages, quarter summary, "what the board should decide this meeting"), **Cadence** (reporting rhythm, reminders, consistency score, overdue flag), plus cohort benchmarking and variance/sensitivity on the KPI board. No worker endpoints exist for packs, cadence reminders or a send log. |
| Mobile Canvas | chrome | — (responsive spec across all authed surfaces) | — (the app is responsive but has no mobile-specific IA) | n/a — pure client | NEW | Five artboards: sidebar → **4 bottom tabs + More**, hub **lens chips** replacing vertical stacking, per-archetype detail reflow (WORK BOARD/LEDGER/COLLECTION vs FEED/MATCH ENGINE/ANALYTICS), AI rail → **FAB + bottom sheet**, and card/gesture rules (whole-card tap target, swipe-back, pull-to-refresh on FEED/LEDGER only, long-press for hover, bottom sheets for every popover, safe areas). Live mobile is a hamburger + `lg:hidden` slide-in drawer in `App.jsx` only — no bottom bar, no sheets, no gestures, no safe-area handling. |
| Navigation Shell | chrome | — (`frontend/src/sidebarConfig.js` + `SidebarNav` in `App.jsx`) | rendered on every authed route | `personas.ts`, `settings.ts`, `billing.ts` (tier gates) | UPGRADE | Live already has: 4 role navs, collapsible groups, desktop collapsed rail, mobile drawer, tier lock + `PaywallModal`, company switcher, sidebar search. Canvas adds: a **"Preview" item state** (dashed outline, "sample data — not built yet", *Notify me at launch*) distinct from the amber tier-lock; **per-item count badges** (e.g. Introductions ·3); an **icon-only collapsed rail with a hover flyout** (live collapsed rail shows abbreviated text, no flyout); a **tier upsell sheet with an "includes" list** per plan (Growth/Studio/Fund); per-role accent colour. Also note the canvas still models Growth/Research as Preview, which live has already shipped (as mock shells — see corrections). |
| Network | shared | `/advisor/network/{introductions,relationships,organizations}` (existing) | those three routes now redirect to `/network` → `NetworkPage` (D10) | `introductions.ts` (propositions, credit ledger, packs), `contacts.ts`, `partnernet.ts`, `network.ts` | RESKIN | Services are live but the shipped workspace predates the canvas *and is not wired to them*: `RelationshipsPage.jsx` renders entirely from `frontend/src/data/advisor/network.js` (0 API calls); `OrganizationsPage.jsx` calls `/api/organizations*` and `IntroductionsPage.jsx` calls `/api/network-introductions/*` — **neither exists on the Worker** (both are in `scripts/api-drift-baseline.json`). Only `/network?tab=introductions` (`IntroductionsPanel`) actually uses live `introductions.ts`. Canvas wants: credits pill + "Get more", accept/pass candidate cards with mutuals + why, a searchable relationship table with last-touch warmth and tags, orgs as a lens with "suggest an org". **CORRECTION 2026-08-29 — the "shipped workspace" this row describes no longer exists.** `AdvisorNetworkWorkspace.jsx`, `RelationshipsPage.jsx`, `OrganizationsPage.jsx`, `IntroductionsPage.jsx` and `frontend/src/data/advisor/network.js` were all DELETED under DECISIONS.md D10, and the three `/advisor/network/*` routes now `<Navigate>` to `/network`. The two phantom endpoints this row flags (`/api/organizations*`, `/api/network-introductions/*`) therefore have no caller left. `frontend/test/network_consolidated.test.mjs` fails the build if any of it returns. What survives is `/network` with `IntroductionsPanel` on live `introductions.ts` — so the canvas work (credits pill, accept/pass cards with mutuals and reasons, a searchable relationship table with last-touch warmth, orgs as a lens) is still to do, but it is an addition to `/network`, not a rescue of a dead stack. |
| Notifications | shared | `/settings/notifications` (existing prefs) + `/notifications` (new full page) | `NotificationBell` component + `/settings/notifications` → `SettingsPage` | `notifications.ts` (`/`, `/unread-count`, `/mark-read`, `/read-all`, `/prefs`, `/:id/read`, `/:id`) | UPGRADE | Shipped bell is a flat list with mark-all + a settings link. Canvas adds: **three tabs grouped by day** in the 380px panel; a **full page** (`/notifications` — no such route today) with type filters, inline "action needed" affordances and a preferences rail; a **6-type vocabulary** (Signatures, Capital, Deal flow, Messages, Events, System) with per-type delivery mode; **digest frequency that batches but never suppresses**, and two types explicitly unmutable (83(b) window, voided envelope). Also: `/api/notifications/push/*` (subscribe/vapid-key/test) is called by the bell but has **no worker route** (drift baseline). |
| Office Hours | founder (Spin-Out Lab) | `/spinout-lab/office-hours` (existing — do not move) | `/spinout-lab/office-hours` → `SpinoutLabOfficeHoursPage` | `advisors.ts`, `partner_office_hours.ts`, `consultations.ts`, `calendar.ts`, `spinout_lab.ts`, `partners.ts` | OUT OF SCOPE | This is the Lab's Week-3 tool page, not a platform-wide surface ("Unlocked · Wk 3", "Week 4 starts soon", open Incorporate blocker). `SpinoutLabOfficeHoursPage.jsx` implements it faithfully and cites this canvas by line number in its header comments ("Office Hours.dc.html L283-285", "design L286", "Design objective list (L328)"), wired to real partner directory + bookings + week state. Judgement call: "Office Hours" is not on the brief's literal OUT-OF-SCOPE enumeration, but it is a Spin-Out Lab canvas and the Lab is live and plugged. If you prefer, CURRENT is the alternative — either way, nothing to build. (`/office-hours` is the separate **advisor-side** availability manager, `OfficeHoursPage`, guarded `admin/advisor`.) |
| Pages · Advisor Cohorts | advisor | `/advisor/cohorts/{founders,guidance,this-week,calendar,outcomes}` (new) | — (no advisor-facing cohort surface) | Would need advisor-scoped reads over `spinout_lab.ts`, `spinout_certificates.ts`, `advisors.ts`, `calendar.ts`; `admin_cohort.ts` exists but is admin-only | NEW | Adds a whole new **"Cohorts" row** to the advisor shell (canvas advisor shell = Home · Practice · Cohorts · Expertise · Network · Research · Practice Settings; live advisor shell = Home · Network · Advisory · Growth · Research · Engagements). Five detail pages: assigned founders with week state/progress/flags, guidance queue + cross-cohort archive, this-week module + materials archive, a calendar that shows cohort/paid-work collisions, and consent-gated graduate outcomes. Cohort tooling today exists **only** under `/admin/spinout-lab` (`AdminCohortTiming`, `AdminCohortApplications`). |
| Pages · Founder Build | founder | `/execution` (+ `/execution/board`, `/execution/roadmap`, `/build/metrics`) | `/execution` → `ExecutionPage` (Projects + Pipeline board + Roadmap stacked); `/build/metrics` → `MetricsPage` | `pipeline.ts`, `progress.ts` (roadmap/OKR/mvp-scope), `projects.ts`, `metrics.ts` | UPGRADE | Five detail pages proposed at `/build/{this-week,board,roadmap,cadence,kpi}` — none of those paths exist. **This week** (weekly commitments + carry-over + retro archive) and **Cadence** (ritual scheduler, review archive, templates) have no live equivalent at all. **Board** adds swimlanes/WIP limits/automations to a board that today is Now/Next/Later + AI decision gates. **Roadmap** adds a timeline, dependency chains and saved scenarios. **KPI entry** adds bulk entry, imports, metric definitions, targets and full history over the current single-month form. |
| Pages · Founder Grow | founder | `/founder/growth/{talent,customers,partnerships,capital,experts}` (existing) | those five routes → `pages/growth/GrowthWorkspace.jsx` | Live but unused by this page: `jobs.ts`, `matches.ts`, `needs.ts`, `partnernet.ts`, `capital.ts`, `brand.ts`, `comarketing.ts`, `events.ts` | RESKIN | The shipped Growth workspace is a **mock-data shell**: every tab reads `frontend/src/data/growth.js` and makes **zero API calls** (the file's own header says "This is a UI shell only"). Canvas has 7 zones vs the shipped 5, adding **Focus** (the month's metric + experiment log with an honest did-it-move read), **Brand** (pages, per-page conversion, captured leads with source) and **Launch** (calendar, co-marketing commitments, article pipeline); it drops "Experts" and renames Capital → Capital match. Needs real wiring, not just restyling. |
| Pages · Founder Network | founder | `/advisor/network/{relationships,introductions,organizations}` (existing) | those three routes now redirect to `/network` (D10) | `contacts.ts`, `introductions.ts`, `partnernet.ts` | RESKIN | Same dead/mock wiring as the Network row above. Detail-layer additions specific to this canvas: a **relationship book with strength, notes, reminders and an explicit going-cold flag**; an **introductions ledger** recording every ask and offer, who carried it and how it ended — including the ones that quietly died; **organizations treated as a lens over the book** rather than a second address book. **CORRECTION 2026-08-29 — this row was stale and said the opposite of the code.** `AdvisorNetworkWorkspace` and `AdvisorResearchWorkspace` no longer exist: the mock stack was DELETED under DECISIONS.md D10, and `/advisor/network{,/introductions,/relationships,/organizations}` and `/advisor/research{,/market}` now `<Navigate>` onto the working surfaces (`/network`, `/market-intel` — the latter making ~32 live calls). `frontend/test/network_consolidated.test.mjs` fails the build if the dead files reappear or a redirect is removed. Read as written, this row would have sent someone to re-wire a component that was deliberately removed. The detail-layer ideas below (relationship strength and going-cold flags, an introductions ledger recording the asks that quietly died, organizations as a lens rather than a second address book) are still unbuilt and still worth building — but against `/network`, not against a workspace that is gone. |
| Pages · Founder Raise | founder | `/raise/pitch`, `/raise/capital`, `/raise/legal-engine`, `/liquidity` (existing) | `PitchWorkspacePage`, `CapitalWorkspacePage`, `LegalEnginePage`, `LiquidityPage` | `decks.ts`, `deck_share_actions.ts`, `captable.ts`, `financials.ts`, `contacts.ts` (raise-prospects/closes/pro-rata), `legal.ts`, `legalcap.ts`, `esign.ts`, `compliance.ts`, `liquidity.ts`, `files.ts`, `dd.ts` | UPGRADE | Six zones; two have no live route. **Status** (`/raise/status`) is a raise war-room — every blocker with what it holds up, investor list by stage, pace-vs-close timeline — nothing like it ships. **Data room** (`/raise/data-room`) is a founder-side **permission matrix** (artifact × investor, grant state, view analytics, stage gap analysis); today the only data room is `GET /deals/:id/data-room`, an export bundle. **Pitch** adds per-investor share analytics incl. which slide people stop on (`/decks/:id/engagement` gives per-token views + read_seconds only, no per-slide dwell). **Capital** adds scenario modelling, the 409A record and an exit waterfall. **Legal** adds an agreement library + signature archive. |
| Pages · Founder Research | founder | `/advisor/research/{ai,market,companies,funds,documents}` (existing) | those routes now redirect to `/market-intel` → `MarketIntelPage` (D10) | Live but unused by that workspace: `market_intel.ts` (~30 endpoints), `assistant.ts`, `news.ts`, `crunchbase.ts`, `files.ts`, `competitors.ts` | RESKIN | Every tab of the shipped Research workspace reads `frontend/src/data/advisor/research.js` with **zero API calls**, even though `/market-intel` is a genuinely rich live implementation of the same Markets material. Canvas adds **Ask** (research sessions with the sources each answer drew on, so a pitch claim can be traced or retired) and **Library** (per-document Q&A history plus the cached-input cost argument); Companies/Funds gain change-since-last-look and fit-score-with-reasons. **CORRECTION 2026-08-29 — the workspace this row describes was DELETED.** `pages/advisor/research/AdvisorResearchWorkspace.jsx` and `frontend/src/data/advisor/research.js` are both gone (DECISIONS.md D10, and see the D9 correction that withdrew the four unbacked Research tabs); `/advisor/research` and `/advisor/research/market` now `<Navigate>` to `MarketIntelPage`, which is the live implementation this row already called "genuinely rich". There is no zero-API-call workspace left to fix. The canvas additions — **Ask** (research sessions carrying the sources each answer drew on, so a pitch claim can be traced or retired) and **Library** (per-document Q&A history) — remain unbuilt and would be built onto `/market-intel`. |
| Pages · Investor Deals | investor-LP | `/deals`, `/pipeline/screening`, `/pipeline/commit`, `/pipeline/transactions` (existing) | `DealsPage`, `PipelineWorkspace` (Screening/Commit/Transactions are live-wired via `pages/pipeline/livePipeline.js` → `/pipeline/active`) | `deals.ts`, `pipeline.ts`, `scoring.ts`, `ic.ts`, `votes.ts`, `dd.ts`, `esign.ts`, `legalcap.ts` | UPGRADE | **Pipeline** adds configurable stages, saved views, velocity, an SLA stale/overdue flag, unassigned-owner surfacing, deal source (incl. Spin-Out Lab) and a passed-deal ledger with a reason taxonomy. **Screening** adds batch runs, rubric configuration and red-flag rules (today `scoring.ts` scores one project at a time with no editable rubric). **Commit** adds quorum arithmetic, conflict/recusal declarations, allocation math and minutes — `ic.ts` records only vote + rationale. **Closing** adds checklist templates, a wire trail and a signature archive. Note the canvas's `/deals/pipeline` etc. would collide with the live `/deals/:dealId` param route. |
| Pages · Investor Fund | investor-LP | `/funds`, `/funds/capital-calls`, `/funds/accounting`, `/lp-reports` (existing) | `FundOpsWorkspace` (+ `FundsPage.AdminFundsView`, `LPReportingPage`) | `funds.ts` (funds, LPs, LPA, capital-call, distributions, report-periods, per-LP reports), `lp_reports.ts`, `capital.ts`, `kyc.ts` | UPGRADE | **LP registry** adds per-LP share of fund, KYC state, documents and a comms log (today: LP list + LPA on-file flag). **Calls** adds a schedule builder, notice letters, wire tracking and delinquency (today: `POST /funds/:id/capital-call` plus a read-only list). **Accounting** adds a full journal + audit trail per line — and `FundAccountingPage.jsx` + `FundPerformancePage.jsx` are **mock** (`frontend/src/data/fundAnalytics.js`, 0 API calls). **Reporting** adds a pack builder, archive and per-LP delivery state. |
| Pages · Investor Network | investor-LP | `/advisor/network/{relationships,introductions,organizations}` (existing) | those three routes now redirect to `/network` (D10) | `contacts.ts`, `introductions.ts`, `partnernet.ts` | RESKIN | Same dead/mock wiring as the Network row. Investor-specific additions: relationships **typed for this product** (founders met, co-investors, LPs, advisors) carrying **deal context** plus the cold flag; the introductions ledger (ask/offer, who carried it, outcome including the dead ones); organizations as firms-you-deal-with with the people you know inside each. **CORRECTION 2026-08-29 — this row was stale and said the opposite of the code.** `AdvisorNetworkWorkspace` and `AdvisorResearchWorkspace` no longer exist: the mock stack was DELETED under DECISIONS.md D10, and `/advisor/network{,/introductions,/relationships,/organizations}` and `/advisor/research{,/market}` now `<Navigate>` onto the working surfaces (`/network`, `/market-intel` — the latter making ~32 live calls). `frontend/test/network_consolidated.test.mjs` fails the build if the dead files reappear or a redirect is removed. Read as written, this row would have sent someone to re-wire a component that was deliberately removed. The detail-layer ideas below (relationship strength and going-cold flags, an introductions ledger recording the asks that quietly died, organizations as a lens rather than a second address book) are still unbuilt and still worth building — but against `/network`, not against a workspace that is gone. |
| Pages · Investor Portfolio | investor-LP | `/portfolio/positions`, `/portfolio/updates` (existing) | `PortfolioWorkspace` tabs (Health / Updates / Cap Table / Performance / Growth) | `positions.ts` (marks, distributions, analytics, kpi-compliance), `portfolio_updates.ts`, `portfolio.ts` | UPGRADE | **Positions** adds cost basis vs current mark sorted by what needs attention rather than alphabetically. **Updates** adds **parse review** — extracted KPIs presented as editable proposals — plus a chase queue and extraction rules; today `PortfolioUpdatesPage` is manual KPI entry only, no parsing and no chase. **Value-add desk** (`/portfolio/value-add`) is entirely new: intros, hours and board prep logged as work, with promised-vs-delivered made auditable. Also note `PortfolioGrowthPage.jsx` is **mock** (`frontend/src/data/portfolioAnalytics.js`). |
| Pages · Investor Research | investor-LP | `/advisor/research/{ai,market,funds,documents}` (existing) + `/market-intel` | `/advisor/research*` now redirects to `MarketIntelPage` (live, ~32 API calls); the mock workspace was deleted (D10) | Live but unused by that workspace: `market_intel.ts`, `assistant.ts`, `dd.ts`, `files.ts`, `news.ts`, `crunchbase.ts` | RESKIN | Same mock-shell problem as founder Research. Investor-specific new zones: **Diligence pulls** (pull builder, the deal each pull attaches to, and a seam indicator showing what the founder staged *and what they withheld*), **Benchmarking** (builder, saved benchmarks, exportable charts, with an explicit small-n caveat), **Markets** reframed around theses and what each thesis actually sourced, and a **Library** with per-source Q&A history. **CORRECTION 2026-08-29 — this row was stale and said the opposite of the code.** `AdvisorNetworkWorkspace` and `AdvisorResearchWorkspace` no longer exist: the mock stack was DELETED under DECISIONS.md D10, and `/advisor/network{,/introductions,/relationships,/organizations}` and `/advisor/research{,/market}` now `<Navigate>` onto the working surfaces (`/network`, `/market-intel` — the latter making ~32 live calls). `frontend/test/network_consolidated.test.mjs` fails the build if the dead files reappear or a redirect is removed. Read as written, this row would have sent someone to re-wire a component that was deliberately removed. The detail-layer ideas below (relationship strength and going-cold flags, an introductions ledger recording the asks that quietly died, organizations as a lens rather than a second address book) are still unbuilt and still worth building — but against `/network`, not against a workspace that is gone. |
| Pages · Partner Pipeline | partner | `/needs` (existing; `/partner/operations/engagements` for the retainer half) | `/needs` → `NeedsBoardPage` (Browse / My needs / My quotes / Engagements — live-wired); `/partner/operations/*` → mock | `needs.ts` (needs, RFP, quotes, accept/reject/withdraw, engagements start/deliver/cancel/invoice, reviews, `/analytics`), `services.ts`, `matches.ts`, `partner_portal.ts` | UPGRADE | Five zones. **Leads** adds source provenance and need-to-capability scoring with the receipt behind each score, editable capability weights, and a recorded pass reason. **Proposals** adds the full lifecycle (drafts/sent/opened/decided) with win-and-loss reasons and open-count as a first-class signal. **Negotiations** is a new board where every card carries its one named blocker. **Retainers** adds renewal dates and **utilization against scope** (34% = churn risk, 118% = unbilled work). **Analytics** adds win rate, cycle time, forecast and loss patterns broken out by shape. Namespace warning: the canvas puts these at `/pipeline/{leads,proposals,…}`, which collides with the live investor deal pipeline at `/pipeline/{screening,commit,transactions}`. |

## Partner / admin / shared / Lab — part 4 (28 canvases)

| Canvas | Persona | Proposed route | Live route today | Backend it wires to | STATUS | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Partner Operator Canvas | partner | `/partner/operations/*` | `/studio`, `/spinout-lab`, `/needs`, `/matches`, `/partner/insights`, `/partner/operations/*`, `/services`, `/perks`, `/comarketing`, `/partner/office-hours`, `/network`, `/market-intel` | `partner_portal.ts`, `partners.ts`, `partnernet.ts`, `needs.ts`, `services.ts`, `comarketing.ts`, `market_intel.ts` | UPGRADE | Seven-row Partner shell is live: Home, Spin-Out Lab, Pipeline, Delivery, Offers, Network, Research. The headerless sidebar keeps existing deep links under their owning workspace. Pipeline, Delivery, Offers, Network, and Research share Partner-only amber identity, cyan source/grant seams, responsive dark-mode framing, and honest access guidance while retaining their existing live API data, actions, guards, and role branches. Home and Spin-Out Lab remain intentionally unchanged. |
| PartnerRail | chrome | — (component, not a route) | Partner workspace side rail | `assistant.ts`, `monitoring.ts` | UPGRADE | The Partner shell now carries the honest AI-assist, consent/screening, founder-grant, and source-provenance guidance rail. It does not invent spend, model, or action controls: those remain unavailable until a scoped Partner rail API exists. |
| Perks & Products | shared (founder + partner) | `/perks` (new) | `/perks` → `PerksPage` (Wave 4) | — (none) for perks/credits; adjacent `products.ts`, `catalog.ts`, `admin_promos.ts`, `billing.ts` | NEW | Credit-priced partner perk marketplace: claim modal with credit debit + redemption code, My Perks + credit ledger, partner submit with live preview + review queue, listing analytics. Distinct from `/marketplace` (services) and `/plans-and-pricing` (Stripe storefront). No perk or credit-ledger table exists. **Wave 4 shipped** it: migration 186 (`perks`, `perk_claims`, `perk_credit_ledger`, `perk_views`), `routes/perks.ts`, and `/perks` role-branched inside one route — founders browse and claim, partners submit and see their own listing stats, admins review. A perk is priced one of three ways: `credits` debits the ledger, `tier` is gated by the SAME `userMeetsTier` the rest of the product uses rather than a second notion of entitlement, and `money` quotes `price_cents` the partner invoices offline. Balance is `SUM(delta)` and is never stored; a claim and its debit go in one `DB.batch`, and two unique indexes (`perk_claims(perk_id, user_id)`, `perk_credit_ledger(user_id, kind, source_ref)`) make a double-spend unreachable rather than unlikely. **A second credit ledger, deliberately:** `intro_credit_ledger` (migration 150) is a different unit — one intro credit buys one warm introduction, where the canvas prices perks at 150–3,200 — so sharing the pool would impose an exchange rate nobody set; and its `bucket`/`kind` CHECK clauses cannot be extended without rebuilding a live table, which an additive migration must not do. **Not built, and why:** no monthly plan allowance. How many credits Growth or Studio includes is a commercial term nobody has set, and inventing one would put a made-up price in the product — the same reason Pricing is blocked in Wave 3. Credits enter only through an admin grant today, and the response carries `allowance_configured` so the page can say "no allowance is set up" instead of "not enough credits", which are very different things to tell someone with a zero balance. The catalogue also starts empty: the partners named in the canvas (Northlake, Sablefin, Oakbridge…) are placeholders, and listing them would invent commercial relationships. |
| Pitch Deck Builder | founder | `/spinout-lab/pitch-deck` | `/spinout-lab/pitch-deck` | `decks.ts`, `deck_share_actions.ts`, `spinout_lab.ts` | OUT OF SCOPE | Lab tool; `components/PitchDeckModals.jsx:3` cites this canvas as its design reference. |
| Portfolio | investor-LP | `/portfolio/health\|updates\|positions\|performance\|growth` | all five via `PortfolioWorkspace` | `portfolio.ts`, `positions.ts`, `portfolio_updates.ts` | UPGRADE | Adds 4 zones with no shipped equivalent: position detail (cash-flow ledger, mark history, KPI small multiples, reserve rail), KPI collection (requested set + compliance strip), alerts, LP export. **`POST /positions/:uid/marks`, `POST /positions/:uid/distributions` and `GET /positions/kpi-compliance` are live and called by no page.** |
| Pricing | public | `/pricing` | `/pricing` | — (none; tiers static in `data/pricing.js`) | UPGRADE | A repositioning, not a restyle: **"There is no price list. There is an application."** Three by-application offerings, zero dollar figures, a platform-surface comparison matrix with scoped chips, an always-open FAQ stating instrument/equity/cap. Conflicts with the live paywall and `data/pricing.js`. Does not touch `/plans-and-pricing` (the Stripe storefront). **Not built — a commercial decision, not a wiring task.** Deleting every dollar figure from `/pricing` while a Stripe storefront sells tiers at `/plans-and-pricing` and `requireTier` gates features on them would leave the site telling one story and the product charging under another. Needs an explicit call on positioning before any code moves. |
| Profiling | founder | `/spinout-lab/profiling` | `/spinout-lab/profiling` | `profiling.ts`, `assessment.ts`, `values.ts`, `skills.ts` | OUT OF SCOPE | Lab tool, Wk 1. |
| Quarterly Report | investor-LP | `/lp-reports` | `/lp-reports`; LP side `/lp-portal` | `lp_reports.ts`, `funds.ts`, `positions.ts` | UPGRADE | Live is a CRUD list of report rows. Canvas is the rendered **4-page LP document**: cover + GP letter + per-LP capital account, portfolio review, cohort telemetry, since-inception Called/NAV/TVPI/DPI/RVPI/Net-IRR table, fees & expenses, valuation-basis notes, per-page confidentiality footer. **`api.fundsLpReport()` exists and is called by no page.** |
| Refer & Earn | shared | `/referrals` | `/referrals` (`/refer` redirects in) | `refer_earn.ts` | UPGRADE | Live has link + legacy code, per-platform share, paste-CSV import, categories, pipeline, FAQ, detail drawer. Canvas adds a QR code with Download PNG, file-upload CSV with a parsed preview and per-contact personalised invite links + status, an Import-from-LinkedIn path, a reward-logic matrix, and the invite-only Referral Partner Program CTA (`POST /refer-earn/strategic-access` exists; no UI). |
| Revenue | founder | `/spinout-lab/revenue` | `/spinout-lab/revenue` | `spinout_lab.ts`, `financials.ts`, `metrics.ts` | OUT OF SCOPE | Lab tool, Wk 3. |
| Round Manager | founder | `/raise/capital/pipeline` | `/raise/capital/pipeline` | `contacts.ts` (`/raise-prospects`, `/raise-round`, `/raise-closes`, `/raise-pro-rata`, `/raise-updates`) | UPGRADE | Tab-for-tab match with the live page — `RaisePipelinePage.jsx:116` says `// Round Manager (#129) — ladder \| closes \| prorata`, and `RoundClosesPanel`/`ProRataPanel` ship. New: per-column weighted subtotals, per-investor certainty/signal/pass-reason, an engagement-detail panel (deck opens, data-room time-per-section, docs viewed), a paper trail (send → sign → wire) with "Mark done & advance", a "Needs a nudge" rail. **Treating this as NEW would be a rebuild.** |
| Scoring Engine v2 | investor-LP / partner / admin | `/scoring` | `/scoring` (`ScoringPage.jsx`, guarded admin/partner/investor) | `scoring.ts`, `ic.ts`, `votes.ts` | UPGRADE | **Does not supersede v1 — different persona, different surface.** Adds six tabs: Configuration (stage presets, editable weights totalling 100%, saved scorecards), Scoring (integrity flags, sub-score × weight contribution), Panel (4 partners score blind, per-criterion spread, divergence + resolve notes), Benchmark (percentile vs a cohort matched on stage/sector/quarter), History (runs pinned to rubric version), IC memo. None of the rubric/panel/benchmark/history concepts exist in `scoring.ts`. |
| Scoring Engine | founder | `/spinout-lab/scoring` | `/spinout-lab/scoring` | `scoring.ts`, `spinout_lab.ts` | OUT OF SCOPE | Lab readiness tool, already built from this exact file (`SpinoutLabScoringPage.jsx:1-4`); ported components in `components/scoring/`. |
| Send for Signature | shared | `/legal/send` (new) | — (only the signer view `/esign/:token` exists) | `esign.ts` (`POST /legal/esign/send`, list, detail, document, forward), `legal.ts` | NEW | 3-step sender wizard → envelope status (per-signer state, remind, copy link, void, audit trail, download executed PDF). **Backend is complete and `api.js` already exposes `esignSend`/`esignList`/`esignDetail`/`esignDocumentUrl` — no page calls any of them.** This is task #119. |
| Signals | founder (+ advisor mode) | `/signals` | `/signals` | `signals.ts`, `market_intel.ts` | UPGRADE | Reframes signals as **opportunity clusters ranked by evidence weight, not recency**: a four-independent-source minimum with a real "we would rather show nothing" empty state, per-cluster coverage/weight/recency, "Inferred — not directly observed" marking; in the drawer the literal source **query** per evidence line, **counter-evidence**, interpretation fenced off from evidence, interview questions, hand-offs, a source-taxonomy/licensing modal. No cluster, coverage, counter-evidence or query fields exist in `signals.ts`. |
| Spin-Out Lab Workspace | founder | `/spinout-lab` | `/spinout-lab` | `spinout_lab.ts`, `progress.ts` | OUT OF SCOPE | The 232 KB master canvas: workspace shell + all 22 tool pages inline. |
| Spin-Out Lab-print-1vkgcux | public | `/spinout-lab` | `/spinout-lab` (logged-out) | `spinout_lab.ts`, `public.ts` | OUT OF SCOPE | **Byte-for-byte duplicate of `Spin-Out Lab.dc.html`** plus a print harness (identical 45,145-char body, difflib ratio 1.0). Redundant. |
| Spin-Out Lab-print | public | `/spinout-lab/brief` | `/spinout-lab/brief` | `spinout_lab.ts` | OUT OF SCOPE | **Not** a duplicate of the above. A truncated 4,547 B fragment of the printable Program Brief that stops mid-element and carries no `x-dc` data block, so every binding is unresolvable. |
| Spin-Out Lab | public | `/spinout-lab` | `/spinout-lab` | `spinout_lab.ts`, `spinout_moderation.ts`, `public.ts` | OUT OF SCOPE | Public Lab landing + cohort tracker + apply flow. |
| Studio Ops | founder | — (see note) | — (deleted from main 2026-08-25) | — (none) | OUT OF SCOPE | Lab tool by subject — Week-2 unlocked tool and Week-2 deliverable in the master workspace canvas — but there is **no live Studio Ops surface**: the route, both pages, the worker route, the service and 16 `api.js` methods were deleted, and `frontend/test/studio_ops_removed.test.mjs` now asserts they stay gone. **Resolved (DECISIONS.md D1): honour the deletion, drop the canvas.** Not a work item in any phase. If Studio Ops returns it should be scoped fresh, not restored from this canvas. |
| Support Security · Super | super-admin | Support → `/tickets`; Security → `/admin/security` (new) | `/tickets` (partial), `/monitoring`, `/admin` | `tickets.ts`, `monitoring.ts`, `monitoring_analytics.ts`, `trust.ts`, `admin.ts` | NEW | Two artboards, two answers. HQ Support = three queues (escalations, HQ-held users, subsidiary admins raising tickets about the admin product), tenant × queue analytics, GitHub `ticket_sync_events` health, inherited SLA bands. Security = a new destination: a `security_events` ledger (the canvas calls it "the one real backend build"), session revoke/force-re-auth, AI-safety counters, GDPR DSR clocks per territory, backup/DR **drill** status, with `admin_audit_log` demoted to one zone of eight. Gated on the absent tenancy model. **UPDATE 2026-08-29 (Wave 4):** the licence LEDGER now exists (migration 187, `/admin/licences`) — territories, seats, terms and renewals per subsidiary. This row stays gated: it needs the SCOPING half, where an account names the licence it belongs to, and no row carries one. |
| Support · Subsidiary | admin | `/tickets` | `/tickets` | `tickets.ts` | UPGRADE | Turns a flat list into a territory-scoped help desk: SLA-chipped inbox with lanes, live chat routed by tenant, escalate-to-HQ writing an engineering ticket under the operator's name, canned replies with usage counts, CSAT, an Eadwyn triage proposal. Its second artboard deliberately **refuses** a tenant-tier security console. Gated on the absent tenancy model. **UPDATE 2026-08-29 (Wave 4):** the licence LEDGER now exists (migration 187, `/admin/licences`) — territories, seats, terms and renewals per subsidiary. This row stays gated: it needs the SCOPING half, where an account names the licence it belongs to, and no row carries one. |
| System Sheet | chrome | — (design-system reference) | — | — (none) | NEW | S1 Tokens + in-use component inventory; S2 the four states the other canvases omit (empty, loading, error, confirmation) plus a voice do/don't. **This is the file the baseline palette was transcribed from.** Partially exists as code (`index.css` `@theme`, `EmptyState.jsx`, `ErrorState.jsx`, `Skeleton.jsx`, `useToast.js`) but there is no published sheet. |
| Team · Authority | admin / super-admin | `/admin/team` (absorbs) + `/ic` | `/admin/team` (public team-page CRUD only), `/ic`, `/ic/:uid` | `admin_team.ts`, `team_public.ts`, `ic.ts`, `votes.ts`, `users.ts`, `projects.ts` | NEW | 8 artboards enforcing one rule: **title, authority and economics are three independent axes.** Subsidiary Team hub (org chart, IC panel, people directory, external-engagement ledger where access always expires, public team page as a *projection*), Super read-only cross-tenant oversight, member profile with a locked-not-hidden economics section, a roles × surfaces × deal-power matrix edited with a typed reason + audit, an IC console where an open FLAG blocks entry to voting while only VOTE decides, a Partner-gated carry ledger, onboard/offboard flows. Live `/admin/team` only edits the marketing page. |
| Team | founder | `/build/team` | `/build/team` | `projects.ts`, `captable.ts`, `legal.ts`, `advisors.ts`, `jobs.ts`, `cofounder.ts`, `partner_office_hours.ts` | UPGRADE | Live tabs = Advisor / Co-Founder / Jobs. Canvas = Roster / Advisors / Hiring / **Coverage**. New: the roster itself (equity %, shares, vesting progress, per-person paperwork status, person drawer with employment + documents + offboard, invite drawer issuing type-appropriate paperwork), an option-pool panel, advisor grants + vesting + cadence, and a whole Coverage tab (inbound from Brand & Pages, co-founder decision state, function-by-function coverage with fixes, quarterly headcount plan, people-cost roll-up feeding Use of Funds). |
| Trust Center v2 | shared | `/trust` | `/trust` | `trust.ts`, `kyc.ts`, `esign.ts`, `settings.ts` | UPGRADE | **Supersedes v1.** Identity/Entity/Accreditation become read-only status reports pointing at Account Settings; adds a multi-company selector (each with its own KYB state); obligations gain provenance + expiry + a real empty state; agreements are grouped with inline expand; sanctions screening gains optional name/DOB/nationality overrides and filter chips. **Independently the right call: the endpoints v1's editable cards POST to do not exist on the worker.** |
| Trust Center | shared | `/trust` | `/trust` | `trust.ts`, `kyc.ts`, `esign.ts` | CURRENT | Shipped page is a faithful build of this canvas. Superseded by v2 — archive; do not build from this file. |
| Use of Funds | founder | `/spinout-lab/use-of-funds` | `/spinout-lab/use-of-funds` | `spinout_lab.ts`, `financials.ts`, `decks.ts` | OUT OF SCOPE | Lab tool, Wk 4. |

---

## Status tally

| Status | Count |
| --- | ---: |
| UPGRADE | 42 |
| OUT OF SCOPE | 27 |
| NEW | 23 |
| RESKIN | 7 |
| CURRENT | 4 |
| DEFERRED | 4 |
| **Total** | **107** |

`UPGRADE` being the largest bucket is the headline: this is mostly a re-integration
pass over surfaces that already exist, not a greenfield build. The 23 `NEW`
entries cluster into four programmes — the AI rail family (8 canvases), the
multi-tenant/licensing tier (5), the detail-layer IA specs (3), and genuinely new
products (Data Room, Messages, Perks, Send for Signature, Team · Authority).

## Corrections to the original brief

1. **`Advisor Studio` is OUT OF SCOPE, not DEFERRED.** Its content is the
   Spin-Out Lab advisor-program application plus the admin access-decisioning
   gate ("Cohort 4 · Week 2"), not a `/studio` persona hub. Only three true
   persona hubs remain deferred, across four files.
2. **`Brand & Landing Page copy.dc.html` is a stale duplicate**, missing three
   `.bl-root.bl-embedded` CSS rules the newer sibling has. Not a distinct surface.
3. **`Spin-Out Lab-print-1vkgcux.dc.html` is a byte-for-byte duplicate** of
   `Spin-Out Lab.dc.html` (identical 45,145-char body) plus a print harness.
   `Spin-Out Lab-print.dc.html` is unrelated to it — a truncated 4,547 B fragment
   of the Program Brief with no data block.
4. **After 2 and 3, the real corpus is 105 distinct surfaces**, not 89 and not 107.
5. **`Compliance` and `Capital` are Lab canvases** even though the brief's
   OUT-OF-SCOPE enumeration does not name them, and both collide by name with a
   different platform-wide surface (`/compliance` the compliance calendar,
   `/capital` the investor page). Do not collapse either pair.
6. **`Office Hours` is the Lab's Week-3 tool**, not the advisor-side
   `/office-hours` availability manager. Two surfaces, one name.

## Live defects found during the audit

These are pre-existing and were not introduced here. Each is a page calling a
path with no worker handler, already recorded in `scripts/api-drift-baseline.json`
(58 `missing_route` entries), which is why CI stays green.

| Surface | Broken calls | Effect |
| --- | --- | --- |
| Network workspace (`/advisor/network/*`) | `/api/network-introductions/*`, `/api/organizations*` | The Network surface every role's sidebar links to. A second, working Network lives at `/network`. |
| Trust Center (`/trust`) | 9 routes: `/trust/kyb/status`, `/kyb/submit`, `/accreditation/*` (5), `/nda/status`, `/nda/:p/preview`, `/nda/sign` | The editable KYB / Accreditation / NDA cards. Trust Center v2 converts exactly these to read-only, so v2 is the fix, not merely a redesign. |
| Marketplace (`/marketplace`) | all 11 `api.marketplace*` calls | The whole page. Implemented only in the dev-only FastAPI. |
| Legal (`/legal`) | `POST /legal/documents/generate` | Document generation. The worker has `POST /templates/:key/generate` and `POST /documents` under different paths. |

Separately, **two code comments block a feature on a premise that is no longer
true**: `SpinoutLabCofounderAgreementPage.jsx:35` and
`components/cofounder/ExecutionConsole.jsx:9` both disable "Send for signature"
because *"api.js has no method to sign a documents row"*. `api.js:1384` has
`esignSend` and `legal.ts:912` has `PUT /documents/:id/sign`.

## Backends that are live with no consumer

Cheap wins — the work is a UI slice, not a build:

- **Send for Signature** — `esign.ts` is complete (envelopes, tokenised signing,
  forward, reject, audit) and `api.js` exposes `esignSend`/`esignList`/
  `esignDetail`/`esignDocumentUrl`. No page calls any of them. (Task #119.)
- **409A safe harbour** — `GET/POST /captable/409a/:projectId` + `/events`, four
  `api.js` methods, zero UI on the cap-table page.
- **Company members** — `POST/DELETE /company/:uid/members` and
  `/company/memberships`, no members UI in Company Settings. Company *creation*
  is the same story: `POST /company/create` works end to end and the switcher's
  button is hardcoded `disabled`.
- **Portfolio marks / KPI compliance** — `POST /positions/:uid/marks`,
  `POST /positions/:uid/distributions`, `GET /positions/kpi-compliance`.
- **Per-LP fund report** — `GET /funds/:id/lp-report/:lpId`, the Quarterly
  Report canvas's exact data source.
- **AI spend** — `aiRouter.ts` already meters per-user daily/monthly and per-org
  caps and per-task cost, surfaced only to admins. This is the data the eight
  rail canvases want user-facing.

## Route-namespace collisions — re-checked, and none of them are collisions

This table previously listed six collisions "to resolve before mounting
anything". Re-verified against `main` and against the installed router, none of
the six is a route collision. Corrected in full, because the original framing
would have bought a redesign nobody needed.

**The load-bearing error: React Router does not match in registration order.**
`react-router-dom` 7.18.2 with `<Routes>` ranks by specificity — a static
segment outranks a dynamic one wherever each is registered. Verified directly
with `matchRoutes`:

```
routes registered as: /deals, /deals/:dealId, /deals/pipeline, /deals/screening
  /deals/pipeline   -> /deals/pipeline      (NOT /deals/:dealId)
  /deals/screening  -> /deals/screening
  /deals/42         -> /deals/:dealId
```

Registration order *does* decide matching in Hono, which is why the worker's
`/api/deals/pass-analytics` had to be mounted above `/api/deals/:id` in #329.
The two systems are not the same and the earlier note conflated them.

| Canvas proposes | Was recorded as | Actually |
| --- | --- | --- |
| `/deals/{pipeline,screening,commit,closing}` | swallowed by `/deals/:dealId` | **Not a collision.** The literals outrank the param. Mount them in any order. |
| `/pipeline/{leads,proposals,…}` (partner) | collides with investor `/pipeline/*` | **Not a collision.** The BD Console canvas's pipeline tabs are `all` / `mine` / `floor` / `retainer` — client-side filters inside one page, not sibling routes, and none share a name with `screening`/`commit`/`transactions`. |
| `/grow/*` | collides with `/founder/growth/*` | **Moot.** #326 withdrew the Growth section; zero `/founder/growth` routes remain. |
| `/research/*` | collides with `/advisor/research/*` | **Not a collision — a duplicate surface.** Distinct paths that render the same material. D8 already settled the market tab (`/advisor/research/market` → `/market-intel`); the rest is the same call, not a routing one. |
| `/fund/*` | collides with `/funds/*` | **Not a collision.** Singular and plural are distinct namespaces. It is a *legibility* hazard — two prefixes one letter apart — and the fix is to not introduce `/fund/*`, not to re-route `/funds/*`. |
| `/practice/*` | collides with `/advisor/advisory/*` | **Not a collision — a duplicate surface.** Same class as `/research/*`. |

So the real question under four of these is D7's, not a routing one: **two URLs
serve the same material — which one survives?** The answer D8 set is the
precedent: keep the surface with the live backend, redirect the other.

Phase 2 is therefore not blocked on a namespace redesign. What it is blocked on
is the duplicate-surface calls above, each of which is a small, independent
decision, and D4 (**resolved: persona roots stay prohibited** — see
`DECISIONS.md`, enforced by `frontend/test/route_namespace_policy.test.mjs`).

## The shell the canvases assume

Every `Pages · *` canvas renders a fixed 9-row sidebar that does not match
`sidebarConfig.js` for any role:

- founder — Home · Validate · Build · Raise · Grow · Network · Research · Trust · Company Settings
- investor — Home · Deals · Portfolio · Axal VC Fund · Fund (locked) · Network · Research · Trust · Firm Settings
- partner — Home · Spin-Out Lab · Pipeline · Delivery · Offers · Network · Research; Company Settings is the pinned footer
- advisor — Home · Practice · Cohorts · Expertise · Network · Research · Practice Settings

Adopting the detail-layer pages implies an IA change, not just new routes. Note
the canvases still model Growth and Research as "Preview", which live has already
shipped — as mock shells.
