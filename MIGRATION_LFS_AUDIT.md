# Git LFS Migration Audit

> Generated from `find . -size +100k -not -path "./node_modules/*" -not -path "./.git/*" -not -path "./.cache/*" -not -path "./.pythonlibs/*"` cross-referenced with `git ls-files`.
> Audit baseline: 2026-05-21. 325 tracked files > 100 KB at audit time.
>
> Sources of truth elsewhere in the repo:
> - `.gitattributes` — the size-gated patterns block.
> - `scripts/lfs-size-gate.mjs` — pre-commit hook enforcing the > 500 KB PDF/PNG rule and the listed-extension rule.
> - `.github/workflows/lfs-budget.yml` — daily budget cron alerting `#ops` at > 80% of the 250 GB Team allotment.
> - `CONTRIBUTING.md` — dev-side workflow including `lfs.fetchexclude`.

## Decision rule

| Destination | When | Rationale |
|---|---|---|
| **LFS** | Versioned *source* artifact that belongs with the codebase: design masters, ML weights, fixture archives, deck-template PSDs that author KB. | We have 250 GB / 250 GB Team allotment idle. Git stays fast; checkouts only fetch pointers; designers and ML engineers get history. |
| **R2** | Runtime-served traffic: user uploads, signed contracts, deck exports, KYC docs, backups. | Zero egress; lifecycle rules; encryption-at-rest via cryptoBox; not source-controlled by intent. |
| **GitHub Releases** | Public point-in-time artifacts: quarterly Investor Signals PDFs, public DD summaries, sector reports. | Uncapped release-asset bandwidth; immutable per tag; doesn't bloat the repo. |
| **Worker bundle** | Tiny atomic assets that ship with the worker: favicons, OG fallback PNG, validation regex JSON, deck templates < 50 KB. | Atomicity with deploys; cold-start latency is the bottleneck, not bytes. |
| **Leave in place** | Anything already in tree that doesn't trip the size gate, or build outputs that GitHub Pages already serves. | No history rewrites. Existing blobs stay until they are rotated by normal turnover. |

History is **NOT** rewritten. `BFG` and `git filter-repo` are explicitly out of scope. New and re-added files only.

## Inventory & decisions

### Build outputs (leave in place; not LFS-eligible)

| Path | Size | Decision | Reasoning |
|---|---|---|---|
| `dist-deploy/index.js` | 2.4 MB | leave-in-place | Worker bundle, deterministically regenerated by `npm run build`. Should arguably be `.gitignore`d (see follow-up); not migrating to LFS because it would bloat LFS bandwidth on every build. |
| `dist-deploy/index.js.map` | 7.7 MB | leave-in-place | Same as above. Source map. |
| `docs/assets/index-*.js` | 3.1 MB + 1.4 MB | leave-in-place | GitHub Pages serves `docs/` directly. Vite hash-named outputs; rotate every build. LFS would churn pointers and waste bandwidth. |
| `docs/assets/index-*.css` | 139 KB | leave-in-place | Same. |
| `assets/`, `assets/assets/` | 4 × ~450 KB | leave-in-place | Legacy GH Pages output; rotates each build. |
| `package-lock.json` | > 100 KB | leave-in-place | Lockfile, must be diffable. Never LFS. |
| `uv.lock` | > 100 KB | leave-in-place | Same. |
| `cloudflare-worker/**/*.json` (1 file > 100 KB) | — | leave-in-place | Schema/manifest; must be diffable. |

### Attached screenshots / PDFs (in-tree; leave; gate future adds)

| Path | Count | Size band | Decision | Reasoning |
|---|---|---|---|---|
| `attached_assets/Screenshot_*.png` | ~280 | 100 KB – 5.7 MB | leave-in-place + **size-gate future adds** | These are agent chat uploads, already committed. Rewriting history is out of scope. The pre-commit hook (`scripts/lfs-size-gate.mjs`) rejects any **new** PNG > 500 KB unless LFS-tracked. |
| `attached_assets/IMG_*.{png,PNG,jpg}` | ~13 | 437 KB – 1.8 MB | leave-in-place + size-gate future adds | Same. |
| `attached_assets/PHOTO-*.{png,jpg}` | 2 | 100 KB – 565 KB | leave-in-place + size-gate future adds | Same. |
| `attached_assets/*.pdf` (Axal VC source decks) | 9 | 100 KB – 1.4 MB | leave-in-place + **LFS for future re-uploads** | Founding-deck PDFs already in tree. Future replacements / new deck PSDs go through LFS (`*.psd`) or GitHub Releases (public PDF distributions). |

### Runtime assets in source (leave in place; small enough)

| Path | Size | Decision | Reasoning |
|---|---|---|---|
| `axal-logo.png` (3 copies: root, `frontend/public/`, `docs/`) | 853 KB × 3 | leave-in-place; **track future logo masters via LFS** | The PNG itself is the rendered web logo and stays out of LFS — it has to be served as a raw file by the Worker / GH Pages. The Adobe Illustrator / Figma source files for the logo go to LFS via `*.ai` / `*.fig`. (See "First LFS commit" below.) |
| `frontend/public/axal-og.png` | < 100 KB | leave-in-place | OG fallback; ships with worker. |

### Generated user data (review)

| Path | Size | Decision | Reasoning |
|---|---|---|---|
| `users/1/activity.md` | 410 KB | **flag for follow-up** | This is a per-user activity export checked into the repo with PII (name + email). It does not belong in LFS or R2 — it should not be in git at all. Filed as a follow-up: rotate it out and add `users/*/activity.md` to `.gitignore`. Until rotation, no action here (no history rewrite). |

### Categories targeted for LFS going forward

These extensions are auto-tracked by `.gitattributes` so *new* commits route through LFS:

- **Design sources**: `*.psd`, `*.psb`, `*.ai`, `*.xd`, `*.fig`, `*.sketch`
- **ML weights / model artifacts**: `*.onnx`, `*.safetensors`, `*.pt`, `*.pth`, `*.gguf`
- **Tabular fixtures**: `*.parquet`
- **Archives**: `*.zip`, `*.tar.gz`, `*.tgz`
- **Video**: `*.mp4`, `*.webm`, `*.mov`
- **Audio masters**: `*.wav`, `*.flac`

PDFs and PNGs are **not** auto-tracked by extension because that would route every small inline diagram and every existing in-tree screenshot through LFS pointers (and the repo already has hundreds). Instead, the pre-commit hook size-gates them: any **new** PDF or PNG > 500 KB must be `git lfs track`'d before commit or the hook rejects the push.

### Categories that stay on R2 (NOT in this audit's scope; documented for clarity)

- User-uploaded pitch decks, founder docs, NDAs
- Signed legal documents (incorporation, 83(b), waterfall outputs)
- KYC documents
- DD reports (already AES-GCM via `cryptoBox.encryptBytes`)
- Backup snapshots, DR drill outputs
- Generated deck PDF exports

R2 retains the zero-egress advantage for serve traffic; LFS bandwidth is precious (250 GB/month) and must be reserved for source artifacts that engineers actually pull on clone.

### Categories that go to GitHub Releases

- Quarterly Investor Signals PDFs (public)
- Public DD summary briefs (founder-redacted)
- Sector reports (public)
- Annual transparency report / threat model snapshots

GitHub Releases are tagged, immutable, and have uncapped public-asset bandwidth. They are reachable from the marketing site without consuming LFS budget. Workflow for publishing lives in `CONTRIBUTING.md → "Publishing public artifacts"`.

## First LFS commit (operator action)

To validate the LFS panel and prove the wiring end-to-end, an operator (outside the main-agent sandbox; main-agent bash is git-restricted) needs to run, once, on a workstation or in the Replit Shell tab:

```bash
git lfs install                          # one-time, per machine
mkdir -p design/logo
cp /path/to/axal-logo-master.ai design/logo/axal-logo-master.ai
git add .gitattributes design/logo/axal-logo-master.ai
git commit -m "chore(lfs): track logo master via LFS"
bash scripts/git-sync.sh                 # or the blue Sync button
```

After the push, GitHub → Repo Settings → Git LFS panel should show one
object and a few hundred KB of storage used. That confirms `.gitattributes`
is doing its job and the budget workflow has a non-zero baseline.

## What the daily budget workflow does

`.github/workflows/lfs-budget.yml` runs at 09:00 UTC daily:

1. Queries `GET /repos/{owner}/{repo}` for `size` and the GraphQL endpoint
   for the current month's LFS bandwidth.
2. Computes projected month-end usage = `current_usage / day_of_month * days_in_month`.
3. If `projected > 0.80 * 250 GB`, posts to `SLACK_OPS_WEBHOOK_URL` with
   current, projected, and the top contributors (resolved from `git lfs ls-files --size`).
4. Always writes a one-line summary to the workflow log so a clean day
   leaves a green tick.

The threshold is 80% (200 GB / month). Tune via the `BUDGET_THRESHOLD_PCT`
workflow input.

## Verification checklist

- [x] `MIGRATION_LFS_AUDIT.md` written (this file).
- [x] `.gitattributes` written with size-gated patterns.
- [x] `scripts/lfs-size-gate.mjs` written; rejects > 500 KB PDF/PNG without LFS.
- [x] `.github/workflows/lfs-budget.yml` written; daily cron + Slack alert at 80%.
- [x] `CONTRIBUTING.md` written with `lfs.fetchexclude`, size-gate, "no history rewrites" rules.
- [ ] **Operator**: first design-source LFS commit (steps above). Main-agent sandbox cannot do this — git is sandbox-blocked.
- [ ] **Operator**: add `SLACK_OPS_WEBHOOK_URL` to GitHub Actions secrets (Settings → Secrets and variables → Actions).
- [ ] **Operator** (optional, recommended): install the pre-commit hook via `npm run lfs:install-hook` (added in this task).

## 2026-06-10 follow-up — Bucket A applied (A1–A5)

This section records the post-audit drift and the changes applied to close the gap without history rewrites.

### Post-audit drift (2026-05-21 → 2026-06-10)

- 547 new PNGs were added since the baseline audit.
- 60 oversize files (> 500 KB) now trip `--all` (previously 325 tracked files > 100 KB at audit baseline):
  - 47 PNGs (100 KB – 5.6 MB, mostly `attached_assets/Screenshot_*.png` and `IMG_*.png`)
  - 1 PDF (`attached_assets/AXAL_VC_-_STARTUP_SCORING-merged_*.pdf`, 1.4 MB)
  - 3 `axal-logo.png` copies (root, `frontend/public/`, `docs/`, 853 KB each — intended for Worker/GH Pages serve, so they stay in the regular pack; future logo *masters* go to LFS via `*.ai`/`*.fig`).
  - The remaining `dist-deploy/`, `backend/app.db`, and `users/1/activity.md` were addressed in A2–A4 (see below).

### Applied changes (A1–A5)

| Step | Action | Status | Notes |
|---|---|---|---|
| A1 | Broaden `.gitattributes` LFS patterns (Office, ML/data, DB snapshots, archives, fonts) | **Done** — Task #1. Additive only; 16 existing DOCX + 1 PPTX grandfathered. |
| A2 | `.gitignore` + untrack `dist-deploy/` | **Done** — Task #2. `git rm --cached` kept files on disk. |
| A3 | `.gitignore` + untrack `backend/app.db` | **Done** — Task #2. Dev-only SQLite; real data lives in D1. |
| A4 | Delete `users/1/activity.md` (PII export) | **Done** — Task #2. File removed from tree and disk. |
| A5 | Add `--against=<ref>` mode to `scripts/lfs-size-gate.mjs` | **Done** — Task #3. Enables PR-scoped gating in CI. |
| A6 | Wire `--against` into CI as an `lfs-gate` job | **Done** — Task #3. Added after `drift` job in `ci.yml`. |
| A7 | Update this audit doc | **Done** — Task #3 (this section). |

### What changed in the CI job

The `lfs-gate` job:
- checks out with `fetch-depth: 0` so the merge-base resolves on PR builds,
- fetches the PR base branch with `--depth=1`,
- runs `node scripts/lfs-size-gate.mjs --against=origin/${{ github.base_ref }}`,
- passes only if files touched by the PR are clean (no grandfathered oversize blobs from the audit baseline trigger failures).

### Still pending / needs decision

- **Bucket B** — history rewrite: explicitly declined in the original plan. The 60 oversize files and the 16 existing DOCX + 1 PPTX remain in the regular pack. They will rotate out naturally as the codebase evolves.
- **Bucket C** — structural decisions:
  - `docs/` rotation: GitHub Pages currently serves from `docs/`. A CI-built `docs/` rotation would decouple the committed build output from the marketing site. **Tag:** `needs decision`.
  - `attached_assets/Pasted-*.txt` excerpts: deferred curation decision on whether these excerpts are still needed. **Tag:** `needs decision`.
  - Deck PDFs → GitHub Releases: public decks (Axal VC source decks) currently in `attached_assets/*.pdf` should migrate to Releases for better bandwidth and discoverability. **Tag:** `needs decision`.
