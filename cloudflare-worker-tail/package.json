{
  "name": "studioos-tail",
  "version": "1.0.0",
  "private": true,
  "description": "Tail consumer worker that forwards studioos worker tail events to R2.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260817.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.123.0"
  }
}
